diff --git a/src/server/database/Updater/UpdateFetcher.cpp b/src/server/database/Updater/UpdateFetcher.cpp
index f21841c0b..d81b35cd2 100644
--- a/src/server/database/Updater/UpdateFetcher.cpp
+++ b/src/server/database/Updater/UpdateFetcher.cpp
@@ -16,6 +16,7 @@
  */
 
 #include "UpdateFetcher.h"
+#include "BuiltInConfig.h"
 #include "CryptoHash.h"
 #include "DBUpdater.h"
 #include "Field.h"
@@ -160,9 +161,10 @@ UpdateFetcher::DirectoryStorage UpdateFetcher::ReceiveIncludedDirectories() cons
             moduleList.emplace_back(itr);
 
         // data/sql
+        std::filesystem::path const modulesBasePath = std::filesystem::path(BuiltInConfig::GetSourceDirectory()) / "modules";
         for (auto const& moduleName : moduleList)
         {
-            std::string path = _sourceDirectory->generic_string() + "/modules/" + moduleName + "/data/sql/"; // modules/mod-name/data/sql/
+            std::string path = (modulesBasePath / moduleName / "data/sql").generic_string(); // modules/mod-name/data/sql/
             Path const p{path};
             if (!is_directory(p))
                 continue;
@@ -176,7 +178,8 @@ UpdateFetcher::DirectoryStorage UpdateFetcher::ReceiveIncludedDirectories() cons
                 std::filesystem::path dirPath = itr->path(); // modules/mod-name/data/sql/db-world
                 std::string dirName = dirPath.filename().string(); // db-world
 
-                if (dirName.find(_dbModuleName) == std::string::npos)
+                bool const isPlayerbotsAlias = _dbModuleName == "db_playerbot" && dirName.find("playerbots") != std::string::npos;
+                if (dirName.find(_dbModuleName) == std::string::npos && !isPlayerbotsAlias)
                     continue;
 
                 DirectoryEntry const entry = { dirPath, AppliedFileEntry::StateConvert("MODULE") };
diff --git a/src/server/game/Battlegrounds/Battleground.cpp b/src/server/game/Battlegrounds/Battleground.cpp
index b10feb7b8..ec8b8d46f 100644
--- a/src/server/game/Battlegrounds/Battleground.cpp
+++ b/src/server/game/Battlegrounds/Battleground.cpp
@@ -361,29 +361,90 @@ inline void Battleground::_ProcessResurrect(uint32 diff)
 
     _reviveEvents.Update(diff);
 
+    // Sync dead players near spirit guides with the resurrect queue
+    for (auto const& itr : GetPlayers())
+    {
+        Player* player = ObjectAccessor::FindPlayer(itr.first);
+        if (!player || player->IsAlive() || !player->IsInWorld())
+            continue;
+
+        // Check if player is already in m_ReviveQueue
+        bool alreadyInQueue = false;
+        for (auto const& queuePair : m_ReviveQueue)
+        {
+            for (ObjectGuid const& val : queuePair.second)
+            {
+                if (val == player->GetGUID())
+                {
+                    alreadyInQueue = true;
+                    break;
+                }
+            }
+            if (alreadyInQueue)
+                break;
+        }
+
+        if (alreadyInQueue)
+            continue;
+
+        // Find a friendly, alive spirit guide in range (30 yards) and queue the player
+        for (ObjectGuid const& creatureGuid : BgCreatures)
+        {
+            Creature* sh = FindBgMap() ? FindBgMap()->GetCreature(creatureGuid) : nullptr;
+            if (!sh || !sh->IsAlive() || !sh->IsSpiritService())
+                continue;
+
+            TeamId guideTeam = (sh->GetEntry() == BG_CREATURE_ENTRY_A_SPIRITGUIDE) ? TEAM_ALLIANCE : TEAM_HORDE;
+            if (guideTeam == player->GetBgTeamId() && player->IsWithinDistInMap(sh, 30.0f))
+            {
+                AddPlayerToResurrectQueue(sh->GetGUID(), player->GetGUID());
+                break;
+            }
+        }
+    }
+
     m_LastResurrectTime += diff;
+
+    // Send time update to all queued players every 0.5 seconds (500ms) to keep client in sync and handle loading screen packet loss
+    if ((m_LastResurrectTime / 500) != ((m_LastResurrectTime - diff) / 500))
+    {
+        for (auto const& queuePair : m_ReviveQueue)
+        {
+            for (ObjectGuid const& playerGuid : queuePair.second)
+            {
+                if (Player* player = ObjectAccessor::FindPlayer(playerGuid))
+                {
+                    sBattlegroundMgr->SendAreaSpiritHealerQueryOpcode(player, this, queuePair.first);
+                }
+            }
+        }
+    }
+
     if (m_LastResurrectTime >= RESURRECTION_INTERVAL)
     {
         if (GetReviveQueueSize())
         {
             for (std::map<ObjectGuid, GuidVector>::iterator itr = m_ReviveQueue.begin(); itr != m_ReviveQueue.end(); ++itr)
             {
-                Creature* sh = nullptr;
+                Creature* sh = FindBgMap() ? FindBgMap()->GetCreature(itr->first) : nullptr;
                 for (ObjectGuid const& guid : itr->second)
                 {
                     Player* player = ObjectAccessor::FindPlayer(guid);
                     if (!player)
                         continue;
 
-                    if (!sh && player->IsInWorld())
+                    // Verify player is still dead, matches team, and within 30 yards of the spirit guide before resurrecting
+                    TeamId guideTeam = (sh && sh->GetEntry() == BG_CREATURE_ENTRY_A_SPIRITGUIDE) ? TEAM_ALLIANCE : TEAM_HORDE;
+                    if (player->IsAlive() || !sh || guideTeam != player->GetBgTeamId() || !player->IsWithinDistInMap(sh, 30.0f))
                     {
-                        sh = player->GetMap()->GetCreature(itr->first);
-                        // only for visual effect
-                        if (sh)
-                            // Spirit Heal, effect 117
-                            sh->CastSpell(sh, SPELL_SPIRIT_HEAL, true);
+                        player->RemoveAurasDueToSpell(SPELL_WAITING_FOR_RESURRECT);
+                        continue;
                     }
 
+                    // Spirit healer casting visual (only once per healer)
+                    if (sh && itr->second.front() == guid)
+                        sh->CastSpell(sh, SPELL_SPIRIT_HEAL, true);
+
                     // Resurrection visual
                     player->CastSpell(player, SPELL_RESURRECTION_VISUAL, true);
                     m_ResurrectQueue.push_back(guid);
@@ -407,6 +468,7 @@ inline void Battleground::_ProcessResurrect(uint32 diff)
             if (!player)
                 continue;
             player->ResurrectPlayer(1.0f);
+            player->RemoveAurasDueToSpell(SPELL_WAITING_FOR_RESURRECT);
             player->CastSpell(player, 6962, true);
             player->CastSpell(player, SPELL_SPIRIT_HEAL_MANA, true);
             player->SpawnCorpseBones(false);
@@ -1449,6 +1511,7 @@ void Battleground::AddPlayerToResurrectQueue(ObjectGuid npc_guid, ObjectGuid pla
         return;
 
     player->CastSpell(player, SPELL_WAITING_FOR_RESURRECT, true);
+    sBattlegroundMgr->SendAreaSpiritHealerQueryOpcode(player, this, npc_guid);
 }
 
 void Battleground::RemovePlayerFromResurrectQueue(Player* player)
@@ -1726,6 +1789,9 @@ bool Battleground::AddSpiritGuide(uint32 type, float x, float y, float z, float
         // correct cast speed
         creature->SetFloatValue(UNIT_MOD_CAST_SPEED, 1.0f);
         //creature->CastSpell(creature, SPELL_SPIRIT_HEAL_CHANNEL, true);
+        
+        // Force the Spirit Healer to be friendly to all players (Faction 35) to allow cross-faction/mercenary range triggers
+        creature->SetFaction(35);
         return true;
     }
     LOG_ERROR("bg.battleground", "Battleground::AddSpiritGuide: cannot create spirit guide (type: {}, entry: {}) for BG (map: {}, instance id: {})!",
diff --git a/src/server/game/Battlegrounds/BattlegroundQueue.cpp b/src/server/game/Battlegrounds/BattlegroundQueue.cpp
index 169f642ec..5ad912f29 100644
--- a/src/server/game/Battlegrounds/BattlegroundQueue.cpp
+++ b/src/server/game/Battlegrounds/BattlegroundQueue.cpp
@@ -206,6 +206,32 @@ GroupQueueInfo* BattlegroundQueue::AddGroup(Player* leader, Group* group, Battle
     return ginfo;
 }
 
+GroupQueueInfo* BattlegroundQueue::DetachPlayerFromGroup(ObjectGuid guid, TeamId teamId)
+{
+    auto playerItr = m_QueuedPlayers.find(guid);
+    if (playerItr == m_QueuedPlayers.end() || !playerItr->second)
+        return nullptr;
+
+    GroupQueueInfo* ginfo = playerItr->second;
+    if (ginfo->Players.size() == 1)
+    {
+        ginfo->teamId = teamId;
+        return ginfo;
+    }
+
+    auto* playerGinfo = new GroupQueueInfo(*ginfo);
+    playerGinfo->Players.clear();
+    playerGinfo->Players.emplace(guid);
+    playerGinfo->teamId = teamId;
+    playerGinfo->IsInvitedToBGInstanceGUID = 0;
+    playerGinfo->RemoveInviteTime = 0;
+
+    ginfo->Players.erase(guid);
+    playerItr->second = playerGinfo;
+    m_QueuedGroups[playerGinfo->BracketId][playerGinfo->GroupType].push_back(playerGinfo);
+    return playerGinfo;
+}
+
 void BattlegroundQueue::PlayerInvitedToBGUpdateAverageWaitTime(GroupQueueInfo* ginfo)
 {
     uint32 timeInQueue = std::max<uint32>(1, getMSTimeDiff(ginfo->JoinTime, GameTime::GetGameTimeMS().count()));
@@ -279,6 +305,15 @@ void BattlegroundQueue::RemovePlayer(ObjectGuid guid, bool decreaseInvitedCount)
 
     GroupQueueInfo* groupInfo = itr->second;
 
+    if (Player* player = ObjectAccessor::FindConnectedPlayer(guid))
+    {
+        // Only fire the queue-leave event when the player truly left/rejected.
+        // decreaseInvitedCount=false with a pending invite means they accepted and entered the BG.
+        bool acceptedInvite = !decreaseInvitedCount && groupInfo->IsInvitedToBGInstanceGUID;
+        if (!acceptedInvite)
+            sScriptMgr->OnPlayerLeaveBGQueue(player);
+    }
+
     uint32 _bracketId = groupInfo->BracketId;
     uint32 _groupType = groupInfo->GroupType;
 
@@ -819,11 +854,27 @@ void BattlegroundQueue::BattlegroundQueueUpdate(uint32 diff, BattlegroundTypeId
             m_SelectionPools[TEAM_ALLIANCE].Init();
             m_SelectionPools[TEAM_HORDE].Init();
 
-            // call a function that does the job for us
-            FillPlayersToBG(bg, bracket_id);
+            std::map<ObjectGuid, uint32> distribution;
+            if (sScriptMgr->OnBattlegroundQueueDistribution(this, bg, bracket_id, distribution))
+            {
+                // Script handled the distribution!
+                for (auto const& pair : distribution)
+                {
+                    ObjectGuid guid = pair.first;
+                    uint32 teamId = pair.second;
 
-            // now everything is set, invite players
-            InviteAllGroupsToBg(bg);
+                    if (GroupQueueInfo* ginfo = DetachPlayerFromGroup(guid, (TeamId)teamId))
+                        InviteGroupToBG(ginfo, bg, (TeamId)teamId);
+                }
+            }
+            else
+            {
+                // call a function that does the job for us
+                FillPlayersToBG(bg, bracket_id);
+
+                // now everything is set, invite players
+                InviteAllGroupsToBg(bg);
+            }
 
             if (!bg->HasFreeSlots())
                 bg->RemoveFromBGFreeSlotQueue();
@@ -899,8 +950,27 @@ void BattlegroundQueue::BattlegroundQueueUpdate(uint32 diff, BattlegroundTypeId
                 return;
             }
 
-            // invite players
-            InviteAllGroupsToBg(bg);
+            std::map<ObjectGuid, uint32> distribution;
+            if (sScriptMgr->OnBattlegroundQueueDistribution(this, bg, bracket_id, distribution))
+            {
+                // Script handled the distribution! Clear selection pools from CheckNormalMatch
+                m_SelectionPools[TEAM_ALLIANCE].Init();
+                m_SelectionPools[TEAM_HORDE].Init();
+
+                for (auto const& pair : distribution)
+                {
+                    ObjectGuid guid = pair.first;
+                    uint32 teamId = pair.second;
+
+                    if (GroupQueueInfo* ginfo = DetachPlayerFromGroup(guid, (TeamId)teamId))
+                        InviteGroupToBG(ginfo, bg, (TeamId)teamId);
+                }
+            }
+            else
+            {
+                // Default native C++ behavior (CheckNormalMatch already filled m_SelectionPools)
+                InviteAllGroupsToBg(bg);
+            }
 
             bg->StartBattleground();
         }
diff --git a/src/server/game/Battlegrounds/BattlegroundQueue.h b/src/server/game/Battlegrounds/BattlegroundQueue.h
index 34575e539..a76200a3d 100644
--- a/src/server/game/Battlegrounds/BattlegroundQueue.h
+++ b/src/server/game/Battlegrounds/BattlegroundQueue.h
@@ -79,6 +79,7 @@ public:
     bool CheckNormalMatch(Battleground* bgTemplate, BattlegroundBracketId bracket_id, uint32 minPlayers, uint32 maxPlayers);
     bool CheckSkirmishForSameFaction(BattlegroundBracketId bracket_id, uint32 minPlayersPerTeam);
     GroupQueueInfo* AddGroup(Player* leader, Group* group, BattlegroundTypeId bgTypeId, PvPDifficultyEntry const* bracketEntry, uint8 arenaType, bool isRated, bool isPremade, uint32 arenaRating, uint32 matchmakerRating, uint32 arenaTeamId = 0, uint32 opponentsArenaTeamId = 0);
+    GroupQueueInfo* DetachPlayerFromGroup(ObjectGuid guid, TeamId teamId);
     void RemovePlayer(ObjectGuid guid, bool decreaseInvitedCount);
     bool IsPlayerInvitedToRatedArena(ObjectGuid pl_guid);
     bool IsPlayerInvited(ObjectGuid pl_guid, uint32 bgInstanceGuid, uint32 removeTime);
diff --git a/src/server/game/Battlegrounds/BattlegroundScore.h b/src/server/game/Battlegrounds/BattlegroundScore.h
index de0663cdc..e5d906547 100644
--- a/src/server/game/Battlegrounds/BattlegroundScore.h
+++ b/src/server/game/Battlegrounds/BattlegroundScore.h
@@ -102,6 +102,7 @@ struct AC_GAME_API BattlegroundScore
     // For Logging purpose
     virtual std::string ToString() const { return ""; }
 
+    public:
     [[nodiscard]] uint32 GetDeaths() const          { return Deaths; }
     [[nodiscard]] uint32 GetHonorableKills() const  { return HonorableKills; }
     [[nodiscard]] uint32 GetBonusHonor() const      { return BonusHonor; }
@@ -112,6 +113,8 @@ struct AC_GAME_API BattlegroundScore
     [[nodiscard]] virtual uint32 GetAttr4() const { return 0; }
     [[nodiscard]] virtual uint32 GetAttr5() const { return 0; }
 
+    protected:
+
     ObjectGuid PlayerGuid;
 
     // Default score, present in every type
diff --git a/src/server/game/Battlegrounds/Zones/BattlegroundWS.cpp b/src/server/game/Battlegrounds/Zones/BattlegroundWS.cpp
index f5bd8a84f..86a533bb6 100644
--- a/src/server/game/Battlegrounds/Zones/BattlegroundWS.cpp
+++ b/src/server/game/Battlegrounds/Zones/BattlegroundWS.cpp
@@ -195,11 +195,11 @@ void BattlegroundWS::EventPlayerCapturedFlag(Player* player)
     player->RemoveAurasWithInterruptFlags(AURA_INTERRUPT_FLAG_ENTER_PVP_COMBAT);
     RemoveAssaultAuras();
 
-    AddPoints(player->GetTeamId(), 1);
-    SetFlagPicker(ObjectGuid::Empty, GetOtherTeamId(player->GetTeamId()));
-    UpdateFlagState(GetOtherTeamId(player->GetTeamId()), BG_WS_FLAG_STATE_ON_BASE);
+    AddPoints(player->GetBgTeamId(), 1);
+    SetFlagPicker(ObjectGuid::Empty, GetOtherTeamId(player->GetBgTeamId()));
+    UpdateFlagState(GetOtherTeamId(player->GetBgTeamId()), BG_WS_FLAG_STATE_ON_BASE);
 
-    if (player->GetTeamId() == TEAM_ALLIANCE)
+    if (player->GetBgTeamId() == TEAM_ALLIANCE)
     {
         player->RemoveAurasDueToSpell(BG_WS_SPELL_WARSONG_FLAG);
         PlaySoundToAll(BG_WS_SOUND_FLAG_CAPTURED_ALLIANCE);
@@ -217,11 +217,11 @@ void BattlegroundWS::EventPlayerCapturedFlag(Player* player)
     SpawnBGObject(BG_WS_OBJECT_H_FLAG, BG_WS_FLAG_RESPAWN_TIME);
     SpawnBGObject(BG_WS_OBJECT_A_FLAG, BG_WS_FLAG_RESPAWN_TIME);
 
-    UpdateWorldState(player->GetTeamId() == TEAM_ALLIANCE ? WORLD_STATE_BATTLEGROUND_WS_FLAG_CAPTURES_ALLIANCE : WORLD_STATE_BATTLEGROUND_WS_FLAG_CAPTURES_HORDE, GetTeamScore(player->GetTeamId()));
+    UpdateWorldState(player->GetBgTeamId() == TEAM_ALLIANCE ? WORLD_STATE_BATTLEGROUND_WS_FLAG_CAPTURES_ALLIANCE : WORLD_STATE_BATTLEGROUND_WS_FLAG_CAPTURES_HORDE, GetTeamScore(player->GetBgTeamId()));
     UpdatePlayerScore(player, SCORE_FLAG_CAPTURES, 1);      // +1 flag captures
-    _lastFlagCaptureTeam = player->GetTeamId();
+    _lastFlagCaptureTeam = player->GetBgTeamId();
 
-    RewardHonorToTeam(GetBonusHonorFromKill(2), player->GetTeamId());
+    RewardHonorToTeam(GetBonusHonorFromKill(2), player->GetBgTeamId());
 
     if (GetTeamScore(TEAM_ALLIANCE) == _configurableMaxTeamScore || GetTeamScore(TEAM_HORDE) == _configurableMaxTeamScore)
     {
@@ -240,7 +240,7 @@ void BattlegroundWS::EventPlayerDroppedFlag(Player* player)
     if (GetFlagPickerGUID(TEAM_HORDE) != player->GetGUID() && GetFlagPickerGUID(TEAM_ALLIANCE) != player->GetGUID())
         return;
 
-    SetFlagPicker(ObjectGuid::Empty, GetOtherTeamId(player->GetTeamId()));
+    SetFlagPicker(ObjectGuid::Empty, GetOtherTeamId(player->GetBgTeamId()));
     player->RemoveAurasDueToSpell(BG_WS_SPELL_WARSONG_FLAG);
     player->RemoveAurasDueToSpell(BG_WS_SPELL_FOCUSED_ASSAULT);
     player->RemoveAurasDueToSpell(BG_WS_SPELL_BRUTAL_ASSAULT);
@@ -249,7 +249,7 @@ void BattlegroundWS::EventPlayerDroppedFlag(Player* player)
         return;
 
     player->CastSpell(player, SPELL_RECENTLY_DROPPED_FLAG, true);
-    if (player->GetTeamId() == TEAM_ALLIANCE)
+    if (player->GetBgTeamId() == TEAM_ALLIANCE)
     {
         UpdateFlagState(TEAM_HORDE, BG_WS_FLAG_STATE_ON_GROUND);
         player->CastSpell(player, BG_WS_SPELL_WARSONG_FLAG_DROPPED, true);
@@ -273,7 +273,7 @@ void BattlegroundWS::EventPlayerClickedOnFlag(Player* player, GameObject* gameOb
     player->RemoveAurasWithInterruptFlags(AURA_INTERRUPT_FLAG_ENTER_PVP_COMBAT);
 
     // Alliance Flag picked up from base
-    if (player->GetTeamId() == TEAM_HORDE && GetFlagState(TEAM_ALLIANCE) == BG_WS_FLAG_STATE_ON_BASE && BgObjects[BG_WS_OBJECT_A_FLAG] == gameObject->GetGUID())
+    if (player->GetBgTeamId() == TEAM_HORDE && GetFlagState(TEAM_ALLIANCE) == BG_WS_FLAG_STATE_ON_BASE && BgObjects[BG_WS_OBJECT_A_FLAG] == gameObject->GetGUID())
     {
         SpawnBGObject(BG_WS_OBJECT_A_FLAG, RESPAWN_ONE_DAY);
         SetFlagPicker(player->GetGUID(), TEAM_ALLIANCE);
@@ -293,7 +293,7 @@ void BattlegroundWS::EventPlayerClickedOnFlag(Player* player, GameObject* gameOb
     }
 
     // Horde Flag picked up from base
-    if (player->GetTeamId() == TEAM_ALLIANCE && GetFlagState(TEAM_HORDE) == BG_WS_FLAG_STATE_ON_BASE && BgObjects[BG_WS_OBJECT_H_FLAG] == gameObject->GetGUID())
+    if (player->GetBgTeamId() == TEAM_ALLIANCE && GetFlagState(TEAM_HORDE) == BG_WS_FLAG_STATE_ON_BASE && BgObjects[BG_WS_OBJECT_H_FLAG] == gameObject->GetGUID())
     {
         SpawnBGObject(BG_WS_OBJECT_H_FLAG, RESPAWN_ONE_DAY);
         SetFlagPicker(player->GetGUID(), TEAM_HORDE);
@@ -320,7 +320,7 @@ void BattlegroundWS::EventPlayerClickedOnFlag(Player* player, GameObject* gameOb
     if (GetFlagState(TEAM_ALLIANCE) == BG_WS_FLAG_STATE_ON_GROUND && player->IsWithinDistInMap(gameObject, 10.0f) && gameObject->GetEntry() == BG_OBJECT_A_FLAG_GROUND_WS_ENTRY)
     {
         SetDroppedFlagGUID(ObjectGuid::Empty, TEAM_ALLIANCE);
-        if (player->GetTeamId() == TEAM_ALLIANCE)
+        if (player->GetBgTeamId() == TEAM_ALLIANCE)
         {
             UpdateFlagState(TEAM_ALLIANCE, BG_WS_FLAG_STATE_ON_BASE);
             SpawnBGObject(BG_WS_OBJECT_A_FLAG, RESPAWN_IMMEDIATELY);
@@ -353,7 +353,7 @@ void BattlegroundWS::EventPlayerClickedOnFlag(Player* player, GameObject* gameOb
     if (GetFlagState(TEAM_HORDE) == BG_WS_FLAG_STATE_ON_GROUND && player->IsWithinDistInMap(gameObject, 10.0f) && gameObject->GetEntry() == BG_OBJECT_H_FLAG_GROUND_WS_ENTRY)
     {
         SetDroppedFlagGUID(ObjectGuid::Empty, TEAM_HORDE);
-        if (player->GetTeamId() == TEAM_HORDE)
+        if (player->GetBgTeamId() == TEAM_HORDE)
         {
             UpdateFlagState(TEAM_HORDE, BG_WS_FLAG_STATE_ON_BASE);
             SpawnBGObject(BG_WS_OBJECT_H_FLAG, RESPAWN_IMMEDIATELY);
@@ -549,9 +549,9 @@ bool BattlegroundWS::UpdatePlayerScore(Player* player, uint32 type, uint32 value
 GraveyardStruct const* BattlegroundWS::GetClosestGraveyard(Player* player)
 {
     if (GetStatus() == STATUS_IN_PROGRESS)
-        return sGraveyard->GetGraveyard(player->GetTeamId() == TEAM_ALLIANCE ? WS_GRAVEYARD_MAIN_ALLIANCE : WS_GRAVEYARD_MAIN_HORDE);
+        return sGraveyard->GetGraveyard(player->GetBgTeamId() == TEAM_ALLIANCE ? WS_GRAVEYARD_MAIN_ALLIANCE : WS_GRAVEYARD_MAIN_HORDE);
     else
-        return sGraveyard->GetGraveyard(player->GetTeamId() == TEAM_ALLIANCE ? WS_GRAVEYARD_FLAGROOM_ALLIANCE : WS_GRAVEYARD_FLAGROOM_HORDE);
+        return sGraveyard->GetGraveyard(player->GetBgTeamId() == TEAM_ALLIANCE ? WS_GRAVEYARD_FLAGROOM_ALLIANCE : WS_GRAVEYARD_FLAGROOM_HORDE);
 }
 
 void BattlegroundWS::FillInitialWorldStates(WorldPackets::WorldState::InitWorldStates& packet)
diff --git a/src/server/game/Entities/Player/Player.cpp b/src/server/game/Entities/Player/Player.cpp
index f2b3207c7..c6f82368c 100644
--- a/src/server/game/Entities/Player/Player.cpp
+++ b/src/server/game/Entities/Player/Player.cpp
@@ -4408,6 +4408,7 @@ void Player::ResurrectPlayer(float restore_percent, bool applySickness)
     SetByteValue(UNIT_FIELD_BYTES_1, UNIT_BYTES_1_OFFSET_ANIM_TIER, UNIT_BYTE1_FLAG_GROUND);
     RemoveAurasDueToSpell(20584);                           // speed bonuses
     RemoveAurasDueToSpell(8326);                            // SPELL_AURA_GHOST
+    RemoveAurasDueToSpell(2584);                            // SPELL_WAITING_FOR_RESURRECT
 
     if (GetSession()->IsARecruiter() || (GetSession()->GetRecruiterId() != 0))
         SetDynamicFlag(UNIT_DYNFLAG_REFER_A_FRIEND);
diff --git a/src/server/game/Groups/Group.cpp b/src/server/game/Groups/Group.cpp
index e6799fec5..392ce779a 100644
--- a/src/server/game/Groups/Group.cpp
+++ b/src/server/game/Groups/Group.cpp
@@ -2072,6 +2072,7 @@ GroupJoinBattlegroundResult Group::CanJoinBattlegroundQueue(Battleground const*
         if (!member->HasFreeBattlegroundQueueId())
             return ERR_BATTLEGROUND_TOO_MANY_QUEUES;
 
+
         // don't let join if someone from the group is already in that bg queue
         if (member->InBattlegroundQueueForBattlegroundQueueType(bgQueueTypeId))
         {
diff --git a/src/server/game/Handlers/BattleGroundHandler.cpp b/src/server/game/Handlers/BattleGroundHandler.cpp
index f9cbfac32..e856cfa5c 100644
--- a/src/server/game/Handlers/BattleGroundHandler.cpp
+++ b/src/server/game/Handlers/BattleGroundHandler.cpp
@@ -177,12 +177,7 @@ void WorldSession::HandleBattlemasterJoinOpcode(WorldPacket& recvData)
         {
             err = ERR_IN_NON_RANDOM_BG;
         }
-        else if (_player->InBattlegroundQueueForBattlegroundQueueType(BATTLEGROUND_QUEUE_2v2) ||
-            _player->InBattlegroundQueueForBattlegroundQueueType(BATTLEGROUND_QUEUE_3v3) ||
-            _player->InBattlegroundQueueForBattlegroundQueueType(BATTLEGROUND_QUEUE_5v5)) // can't be already queued for arenas
-        {
-            err = ERR_BATTLEGROUND_QUEUED_FOR_RATED;
-        }
+
         // don't let Death Knights join BG queues when they are not allowed to be teleported yet
         else if (_player->IsClass(CLASS_DEATH_KNIGHT, CLASS_CONTEXT_TELEPORT) && _player->GetMapId() == MAP_EBON_HOLD && !_player->IsGameMaster() && !_player->HasSpell(50977))
         {
@@ -231,12 +226,7 @@ void WorldSession::HandleBattlemasterJoinOpcode(WorldPacket& recvData)
             {
                 err = ERR_IN_NON_RANDOM_BG;
             }
-            else if (member->InBattlegroundQueueForBattlegroundQueueType(BATTLEGROUND_QUEUE_2v2) ||
-                member->InBattlegroundQueueForBattlegroundQueueType(BATTLEGROUND_QUEUE_3v3) ||
-                member->InBattlegroundQueueForBattlegroundQueueType(BATTLEGROUND_QUEUE_5v5)) // can't be already queued for arenas
-            {
-                err = ERR_BATTLEGROUND_QUEUED_FOR_RATED;
-            }
+
             else if (member->InBattlegroundQueueForBattlegroundQueueType(bgQueueTypeId)) // queued for this bg
             {
                 err = ERR_BATTLEGROUND_NONE;
@@ -735,11 +725,10 @@ void WorldSession::HandleBattlemasterJoinArena(WorldPacket& recvData)
         return;
 
     // must have free queue slot
-    // pussywizard: allow being queued only in one arena queue, and it even cannot be together with bg queues
-    if (_player->InBattlegroundQueue())
+    if (!_player->HasFreeBattlegroundQueueId())
     {
         WorldPacket data;
-        sBattlegroundMgr->BuildGroupJoinedBattlegroundPacket(&data, ERR_BATTLEGROUND_CANNOT_QUEUE_FOR_RATED);
+        sBattlegroundMgr->BuildGroupJoinedBattlegroundPacket(&data, ERR_BATTLEGROUND_TOO_MANY_QUEUES);
         SendPacket(&data);
         return;
     }
diff --git a/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.cpp b/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.cpp
index 9881a25f0..246cd3a8a 100644
--- a/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.cpp
+++ b/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.cpp
@@ -67,6 +67,12 @@ void ScriptMgr::OnAddGroup(BattlegroundQueue* queue, GroupQueueInfo* ginfo, uint
 bool ScriptMgr::CanFillPlayersToBG(BattlegroundQueue* queue, Battleground* bg, BattlegroundBracketId bracket_id)
 {
     CALL_ENABLED_BOOLEAN_HOOKS(AllBattlegroundScript, ALLBATTLEGROUNDHOOK_CAN_FILL_PLAYERS_TO_BG, !script->CanFillPlayersToBG(queue, bg, bracket_id));
+    return true;
+}
+
+bool ScriptMgr::OnBattlegroundQueueDistribution(BattlegroundQueue* queue, Battleground* bg, BattlegroundBracketId bracket_id, std::map<ObjectGuid, uint32>& distribution)
+{
+    CALL_ENABLED_BOOLEAN_HOOKS_WITH_DEFAULT_FALSE(AllBattlegroundScript, ALLBATTLEGROUNDHOOK_ON_BATTLEGROUND_QUEUE_DISTRIBUTION, script->OnBattlegroundQueueDistribution(queue, bg, bracket_id, distribution));
 }
 
 bool ScriptMgr::IsCheckNormalMatch(BattlegroundQueue* queue, Battleground* bgTemplate, BattlegroundBracketId bracket_id, uint32 minPlayers, uint32 maxPlayers)
diff --git a/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.h b/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.h
index 108f3cf7f..21a2bd6fc 100644
--- a/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.h
+++ b/src/server/game/Scripting/ScriptDefines/AllBattlegroundScript.h
@@ -43,6 +43,7 @@ enum AllBattlegroundHook
     ALLBATTLEGROUNDHOOK_ON_BATTLEGROUND_CREATE,
     ALLBATTLEGROUNDHOOK_CAN_ADD_GROUP_TO_MATCHING_POOL,
     ALLBATTLEGROUNDHOOK_GET_PLAYER_MATCHMAKING_RATING,
+    ALLBATTLEGROUNDHOOK_ON_BATTLEGROUND_QUEUE_DISTRIBUTION,
     ALLBATTLEGROUNDHOOK_END
 };
 
@@ -92,6 +93,8 @@ public:
 
     [[nodiscard]] virtual bool CanFillPlayersToBG(BattlegroundQueue* /*queue*/, Battleground* /*bg*/, BattlegroundBracketId /*bracket_id*/) { return true; }
 
+    [[nodiscard]] virtual bool OnBattlegroundQueueDistribution(BattlegroundQueue* /*queue*/, Battleground* /*bg*/, BattlegroundBracketId /*bracket_id*/, std::map<ObjectGuid, uint32>& /*distribution*/) { return false; }
+
     [[nodiscard]] virtual bool IsCheckNormalMatch(BattlegroundQueue* /*queue*/, Battleground* /*bgTemplate*/, BattlegroundBracketId /*bracket_id*/, uint32 /*minPlayers*/, uint32 /*maxPlayers*/) { return false; };
 
     [[nodiscard]] virtual bool CanSendMessageBGQueue(BattlegroundQueue* /*queue*/, Player* /*leader*/, Battleground* /*bg*/, PvPDifficultyEntry const* /*bracketEntry*/) { return true; }
diff --git a/src/server/game/Scripting/ScriptDefines/PlayerScript.cpp b/src/server/game/Scripting/ScriptDefines/PlayerScript.cpp
index 5ee4be5e1..7051ba5e2 100644
--- a/src/server/game/Scripting/ScriptDefines/PlayerScript.cpp
+++ b/src/server/game/Scripting/ScriptDefines/PlayerScript.cpp
@@ -353,6 +353,11 @@ void ScriptMgr::OnPlayerJoinBG(Player* player)
     CALL_ENABLED_HOOKS(PlayerScript, PLAYERHOOK_ON_PLAYER_JOIN_BG, script->OnPlayerJoinBG(player));
 }
 
+void ScriptMgr::OnPlayerLeaveBGQueue(Player* player)
+{
+    CALL_ENABLED_HOOKS(PlayerScript, PLAYERHOOK_ON_PLAYER_LEAVE_BG_QUEUE, script->OnPlayerLeaveBGQueue(player));
+}
+
 void ScriptMgr::OnPlayerJoinArena(Player* player)
 {
     CALL_ENABLED_HOOKS(PlayerScript, PLAYERHOOK_ON_PLAYER_JOIN_ARENA, script->OnPlayerJoinArena(player));
diff --git a/src/server/game/Scripting/ScriptDefines/PlayerScript.h b/src/server/game/Scripting/ScriptDefines/PlayerScript.h
index b718b81c5..579492da0 100644
--- a/src/server/game/Scripting/ScriptDefines/PlayerScript.h
+++ b/src/server/game/Scripting/ScriptDefines/PlayerScript.h
@@ -98,6 +98,7 @@ enum PlayerHook
     PLAYERHOOK_ON_EQUIP,
     PLAYERHOOK_ON_UNEQUIP_ITEM,
     PLAYERHOOK_ON_PLAYER_JOIN_BG,
+    PLAYERHOOK_ON_PLAYER_LEAVE_BG_QUEUE,
     PLAYERHOOK_ON_PLAYER_JOIN_ARENA,
     PLAYERHOOK_GET_CUSTOM_GET_ARENA_TEAM_ID,
     PLAYERHOOK_GET_CUSTOM_ARENA_PERSONAL_RATING,
@@ -419,6 +420,9 @@ public:
     // After player enters queue for BG
     virtual void OnPlayerJoinBG(Player* /*player*/) { }
 
+    // After player leaves queue for BG
+    virtual void OnPlayerLeaveBGQueue(Player* /*player*/) { }
+
     // After player enters queue for Arena
     virtual void OnPlayerJoinArena(Player* /*player*/) { }
 
diff --git a/src/server/game/Scripting/ScriptMgr.h b/src/server/game/Scripting/ScriptMgr.h
index 9fd4ff356..8677ae12b 100644
--- a/src/server/game/Scripting/ScriptMgr.h
+++ b/src/server/game/Scripting/ScriptMgr.h
@@ -376,6 +376,7 @@ public: /* PlayerScript */
     void OnPlayerEquip(Player* player, Item* it, uint8 bag, uint8 slot, bool update);
     void OnPlayerUnequip(Player* player, Item* it);
     void OnPlayerJoinBG(Player* player);
+    void OnPlayerLeaveBGQueue(Player* player);
     void OnPlayerJoinArena(Player* player);
     void OnPlayerGetMaxPersonalArenaRatingRequirement(Player const* player, uint32 minSlot, uint32& maxArenaRating) const;
     void OnPlayerLootItem(Player* player, Item* item, uint32 count, ObjectGuid lootguid);
@@ -625,6 +626,7 @@ public: /* BGScript */
     void OnAddGroup(BattlegroundQueue* queue, GroupQueueInfo* ginfo, uint32& index, Player* leader, Group* group, BattlegroundTypeId bgTypeId, PvPDifficultyEntry const* bracketEntry,
         uint8 arenaType, bool isRated, bool isPremade, uint32 arenaRating, uint32 matchmakerRating, uint32 arenaTeamId, uint32 opponentsArenaTeamId);
     bool CanFillPlayersToBG(BattlegroundQueue* queue, Battleground* bg, BattlegroundBracketId bracket_id);
+    bool OnBattlegroundQueueDistribution(BattlegroundQueue* queue, Battleground* bg, BattlegroundBracketId bracket_id, std::map<ObjectGuid, uint32>& distribution);
     bool IsCheckNormalMatch(BattlegroundQueue* queue, Battleground* bgTemplate, BattlegroundBracketId bracket_id, uint32 minPlayers, uint32 maxPlayers);
     bool CanSendMessageBGQueue(BattlegroundQueue* queue, Player* leader, Battleground* bg, PvPDifficultyEntry const* bracketEntry);
     bool OnBeforeSendJoinMessageArenaQueue(BattlegroundQueue* queue, Player* leader, GroupQueueInfo* ginfo, PvPDifficultyEntry const* bracketEntry, bool isRated);
