diff --git a/conf/playerbots.conf.dist b/conf/playerbots.conf.dist
index 711d0fff..07736af1 100644
--- a/conf/playerbots.conf.dist
+++ b/conf/playerbots.conf.dist
@@ -359,7 +359,7 @@ AiPlayerbot.MaxMovementSearchTime = 3
 AiPlayerbot.ExpireActionTime = 5000
 
 # Max dispel auras duration
-AiPlayerbot.DispelAuraDuration = 700
+AiPlayerbot.DispelAuraDuration = 2000
 
 # Delay between two bot actions
 AiPlayerbot.ReactDelay = 100
@@ -1004,6 +1004,9 @@ AiPlayerbot.AutoPickTalents = 1
 # Default: 54197 (Cold Weather Flying)
 AiPlayerbot.RandomBotSpellIds = "54197"
 
+# Spell IDs bots must not learn or cast
+AiPlayerbot.DisabledSpellIds = ""
+
 # ID of spell to open lootable chests
 AiPlayerbot.OpenGoSpell = 6477
 
diff --git a/src/Ai/Base/Actions/AcceptInvitationAction.cpp b/src/Ai/Base/Actions/AcceptInvitationAction.cpp
index deb1159a..2459f206 100644
--- a/src/Ai/Base/Actions/AcceptInvitationAction.cpp
+++ b/src/Ai/Base/Actions/AcceptInvitationAction.cpp
@@ -19,6 +19,20 @@ bool AcceptInvitationAction::Execute(Event event)
     if (!grp)
         return false;
     WorldPacket packet = event.getPacket();
+
+    if (!packet.empty())
+    {
+        Player* inviter = ObjectAccessor::FindPlayer(grp->GetLeaderGUID());
+        if (inviter)
+        {
+            WorldPacket data(SMSG_GROUP_DECLINE, 10);
+            data << bot->GetName();
+            inviter->SendDirectMessage(&data);
+        }
+        bot->UninviteFromGroup();
+        return false;
+    }
+
     uint8 flag;
     std::string name;
     packet >> flag >> name;
diff --git a/src/Ai/Base/Actions/AcceptQuestAction.cpp b/src/Ai/Base/Actions/AcceptQuestAction.cpp
index e80bdbe1..054d952c 100644
--- a/src/Ai/Base/Actions/AcceptQuestAction.cpp
+++ b/src/Ai/Base/Actions/AcceptQuestAction.cpp
@@ -99,108 +99,12 @@ bool AcceptQuestAction::Execute(Event event)
     return hasAccept;
 }
 
-bool AcceptQuestShareAction::Execute(Event event)
+bool AcceptQuestShareAction::Execute(Event)
 {
-    Player* master = GetMaster();
-    Player* bot = botAI->GetBot();
-
-    WorldPacket& p = event.getPacket();
-    p.rpos(0);
-    uint32 quest;
-    p >> quest;
-
-    Quest const* qInfo = sObjectMgr->GetQuestTemplate(quest);
-    if (!qInfo || !bot->GetDivider())
-        return false;
-
-    quest = qInfo->GetQuestId();
-
-    if (bot->HasQuest(quest))
-    {
-        bot->SetDivider(ObjectGuid::Empty);
-        botAI->TellError(PlayerbotTextMgr::instance().GetBotTextOrDefault(
-            "quest_already_have_error", "I have this quest", {}));
-        return false;
-    }
-
-    if (!bot->CanTakeQuest(qInfo, false))
-    {
-        // can't take quest
-        bot->SetDivider(ObjectGuid::Empty);
-        botAI->TellError(PlayerbotTextMgr::instance().GetBotTextOrDefault(
-            "quest_cant_take_error", "I can't take this quest", {}));
-
-        return false;
-    }
-
-    if (!bot->GetDivider().IsEmpty())
-    {
-        // send msg to quest giving player
-        master->SendPushToPartyResponse(bot, QUEST_PARTY_MSG_ACCEPT_QUEST);
-        bot->SetDivider(ObjectGuid::Empty);
-    }
-
-    if (bot->CanAddQuest(qInfo, false))
-    {
-        bot->AddQuest(qInfo, master);
-
-        if (bot->CanCompleteQuest(quest))
-            bot->CompleteQuest(quest);
-
-        // Runsttren: did not add typeid switch from WorldSession::HandleQuestgiverAcceptQuestOpcode!
-        // I think it's not needed, cause typeid should be TYPEID_PLAYER - and this one is not handled
-        // there and there is no default case also.
-
-        if (qInfo->GetSrcSpell() > 0)
-        {
-            bot->CastSpell(bot, qInfo->GetSrcSpell(), true);
-        }
-
-        botAI->TellMaster(PlayerbotTextMgr::instance().GetBotTextOrDefault(
-            "quest_accept", "Quest accepted", {}));
-        return true;
-    }
-
     return false;
 }
 
-bool ConfirmQuestAction::Execute(Event event)
+bool ConfirmQuestAction::Execute(Event)
 {
-    Player* bot = botAI->GetBot();
-    Player* requester = event.getOwner() ? event.getOwner() : GetMaster();
-
-    WorldPacket& p = event.getPacket();
-    p.rpos(0);
-    uint32 quest;
-    p >> quest;
-    Quest const* qInfo = sObjectMgr->GetQuestTemplate(quest);
-
-    if (!qInfo)
-        return false;
-
-    quest = qInfo->GetQuestId();
-    if (!bot->CanTakeQuest(qInfo, false))
-    {
-        // can't take quest
-        // botAI->TellError("quest_cant_take");
-        return false;
-    }
-
-    if (bot->CanAddQuest(qInfo, false))
-    {
-        bot->AddQuest(qInfo, requester);
-
-        if (bot->CanCompleteQuest(quest))
-            bot->CompleteQuest(quest);
-
-        if (qInfo->GetSrcSpell() > 0)
-        {
-            bot->CastSpell(bot, qInfo->GetSrcSpell(), true);
-        }
-
-        // botAI->TellMaster("quest_accept");
-        return true;
-    }
-
     return false;
 }
diff --git a/src/Ai/Base/Actions/BattleGroundJoinAction.cpp b/src/Ai/Base/Actions/BattleGroundJoinAction.cpp
index 58ee42cf..712c7eee 100644
--- a/src/Ai/Base/Actions/BattleGroundJoinAction.cpp
+++ b/src/Ai/Base/Actions/BattleGroundJoinAction.cpp
@@ -644,6 +644,9 @@ bool BGLeaveAction::Execute(Event /*event*/)
     if (!(bot->InBattlegroundQueue() || bot->InBattleground()))
         return false;
 
+    if (bot->InBattleground() && bot->GetSession()->IsBot())
+        return false;
+
     // botAI->ChangeStrategy("-bg", BOT_STATE_NON_COMBAT);
 
     if (BGStatusAction::LeaveBG(botAI))
@@ -694,6 +697,9 @@ bool BGStatusAction::LeaveBG(PlayerbotAI* botAI)
     Battleground* bg = bot->GetBattleground();
     if (!bg)
         return false;
+
+    if (bot->GetSession()->IsBot())
+        return false;
     bool isArena = bg->isArena();
     bool isRandomBot = sRandomPlayerbotMgr.IsRandomBot(bot);
 
@@ -972,9 +978,6 @@ bool BGStatusAction::Execute(Event event)
 
     if (statusid == STATUS_IN_PROGRESS)  // placeholder for Leave BG if it takes too long
     {
-        LOG_INFO("playerbots", "Bot {} {}:{} <{}>: Received BG status IN_PROGRESS for {} {}",
-                 bot->GetGUID().ToString().c_str(), bot->GetTeamId() == TEAM_ALLIANCE ? "A" : "H", bot->GetLevel(),
-                 bot->GetName(), isArena ? "Arena" : "BG", _bgType);
         return false;
     }
 
diff --git a/src/Ai/Base/Actions/BuyAction.cpp b/src/Ai/Base/Actions/BuyAction.cpp
index e3d454dd..515a2bac 100644
--- a/src/Ai/Base/Actions/BuyAction.cpp
+++ b/src/Ai/Base/Actions/BuyAction.cpp
@@ -13,242 +13,7 @@
 #include "Playerbots.h"
 #include "StatsWeightCalculator.h"
 
-bool BuyAction::Execute(Event event)
+bool BuyAction::Execute(Event)
 {
-    bool buyUseful = false;
-    ItemIds itemIds;
-    std::string const link = event.getParam();
-
-    if (link == "vendor")
-        buyUseful = true;
-    else
-    {
-        itemIds = chat->parseItems(link);
-    }
-
-    GuidVector vendors = botAI->GetAiObjectContext()->GetValue<GuidVector>("nearest npcs")->Get();
-
-    bool vendored = false;
-    bool result = false;
-    for (GuidVector::iterator i = vendors.begin(); i != vendors.end(); ++i)
-    {
-        ObjectGuid vendorguid = *i;
-        Creature* pCreature = bot->GetNPCIfCanInteractWith(vendorguid, UNIT_NPC_FLAG_VENDOR);
-        if (!pCreature)
-            continue;
-
-        vendored = true;
-
-        if (buyUseful)
-        {
-            // Items are evaluated from high-level to low level.
-            // For each item the bot checks again if an item is usefull.
-            // Bot will buy until no usefull items are left.
-
-            VendorItemData const* tItems = pCreature->GetVendorItems();
-            if (!tItems)
-                continue;
-
-            VendorItemList m_items_sorted = tItems->m_items;
-
-            m_items_sorted.erase(std::remove_if(m_items_sorted.begin(), m_items_sorted.end(),
-                                                [](VendorItem* i)
-                                                {
-                                                    ItemTemplate const* proto = sObjectMgr->GetItemTemplate(i->item);
-                                                    return !proto;
-                                                }),
-                                 m_items_sorted.end());
-
-            if (m_items_sorted.empty())
-                continue;
-
-            StatsWeightCalculator calculator(bot);
-            calculator.SetItemSetBonus(false);
-            calculator.SetOverflowPenalty(false);
-
-            std::sort(m_items_sorted.begin(), m_items_sorted.end(),
-                [&calculator](VendorItem* i, VendorItem* j)
-                {
-                    ItemTemplate const* item1 = sObjectMgr->GetItemTemplate(i->item);
-                    ItemTemplate const* item2 = sObjectMgr->GetItemTemplate(j->item);
-
-                    if (!item1 || !item2)
-                        return false;
-
-                    float score1 = calculator.CalculateItem(item1->ItemId);
-                    float score2 = calculator.CalculateItem(item2->ItemId);
-
-                    // Fallback to itemlevel if either score is 0
-                    if (score1 == 0 || score2 == 0)
-                    {
-                        score1 = item1->ItemLevel;
-                        score2 = item2->ItemLevel;
-                    }
-                    return score1 > score2; // Sort in descending order (highest score first)
-                });
-
-            std::unordered_map<uint32, float> bestPurchasedItemScore;  // Track best item score per InventoryType
-
-            for (auto& tItem : m_items_sorted)
-            {
-                uint32 maxPurchases = 1;  // Default to buying once
-                ItemTemplate const* proto = sObjectMgr->GetItemTemplate(tItem->item);
-                if (!proto)
-                    continue;
-
-                if (proto->Class == ITEM_CLASS_CONSUMABLE || proto->Class == ITEM_CLASS_PROJECTILE)
-                {
-                    maxPurchases = 10;  // Allow up to 10 purchases if it's a consumable or projectile
-                }
-
-                for (uint32 i = 0; i < maxPurchases; i++)
-                {
-                    ItemUsage usage = AI_VALUE2(ItemUsage, "item usage", tItem->item);
-
-                    uint32 invType = proto->InventoryType;
-
-                    // Calculate item score
-                    float newScore = calculator.CalculateItem(proto->ItemId);
-
-                    // Skip if we already bought a better item for this slot
-                    if (bestPurchasedItemScore.find(invType) != bestPurchasedItemScore.end() &&
-                        bestPurchasedItemScore[invType] > newScore)
-                    {
-                        break;  // Skip lower-scoring items
-                    }
-
-                    // Check the bot's currently equipped item for this slot
-                    uint8 dstSlot = botAI->FindEquipSlot(proto, NULL_SLOT, true);
-                    Item* oldItem = bot->GetItemByPos(INVENTORY_SLOT_BAG_0, dstSlot);
-
-                    float oldScore = 0.0f;
-                    if (oldItem)
-                    {
-                        ItemTemplate const* oldItemProto = oldItem->GetTemplate();
-                        if (oldItemProto)
-                            oldScore = calculator.CalculateItem(oldItemProto->ItemId);
-                    }
-
-                    // Skip if the bot already has a better or equal item equipped
-                    if (oldScore > newScore)
-                        break;
-
-                    uint32 price = proto->BuyPrice;
-                    price = uint32(floor(price * bot->GetReputationPriceDiscount(pCreature)));
-
-                    NeedMoneyFor needMoneyFor = NeedMoneyFor::none;
-                    switch (usage)
-                    {
-                        case ITEM_USAGE_REPLACE:
-                        case ITEM_USAGE_EQUIP:
-                        case ITEM_USAGE_BAD_EQUIP:
-                        case ITEM_USAGE_BROKEN_EQUIP:
-                            needMoneyFor = NeedMoneyFor::gear;
-                            break;
-                        case ITEM_USAGE_AMMO:
-                            needMoneyFor = NeedMoneyFor::ammo;
-                            break;
-                        case ITEM_USAGE_QUEST:
-                            needMoneyFor = NeedMoneyFor::anything;
-                            break;
-                        case ITEM_USAGE_USE:
-                            needMoneyFor = NeedMoneyFor::consumables;
-                            break;
-                        case ITEM_USAGE_SKILL:
-                            needMoneyFor = NeedMoneyFor::tradeskill;
-                            break;
-                        default:
-                            break;
-                    }
-
-                    if (needMoneyFor == NeedMoneyFor::none)
-                        break;
-
-                    if (AI_VALUE2(uint32, "free money for", uint32(needMoneyFor)) < price)
-                        break;
-
-                    if (!BuyItem(tItems, vendorguid, proto))
-                        break;
-
-                    // Store the best item score per InventoryType
-                    bestPurchasedItemScore[invType] = newScore;
-
-                    if (needMoneyFor == NeedMoneyFor::gear)
-                    {
-                        botAI->DoSpecificAction("equip upgrades packet action");
-                    }
-                }
-            }
-        }
-        else
-        {
-            if (itemIds.empty())
-                return false;
-
-            for (ItemIds::iterator i = itemIds.begin(); i != itemIds.end(); i++)
-            {
-                uint32 itemId = *i;
-                ItemTemplate const* proto = sObjectMgr->GetItemTemplate(itemId);
-                if (!proto)
-                    continue;
-
-                result |= BuyItem(pCreature->GetVendorItems(), vendorguid, proto);
-
-                if (!result)
-                {
-                    std::ostringstream out;
-                    out << "Nobody sells " << ChatHelper::FormatItem(proto) << " nearby";
-                    botAI->TellMaster(out.str());
-                    continue;
-                }
-
-                ItemUsage usage = AI_VALUE2(ItemUsage, "item usage", itemId);
-                if (usage == ITEM_USAGE_REPLACE || usage == ITEM_USAGE_EQUIP ||
-                    usage == ITEM_USAGE_BAD_EQUIP || usage == ITEM_USAGE_BROKEN_EQUIP)
-                {
-                    botAI->DoSpecificAction("equip upgrades packet action");
-                    break;
-                }
-            }
-        }
-    }
-
-    return vendored;
-}
-
-bool BuyAction::BuyItem(VendorItemData const* tItems, ObjectGuid vendorguid, ItemTemplate const* proto)
-{
-    if (!tItems || !proto)
-        return false;
-
-    uint32 itemId = proto->ItemId;
-    uint32 oldCount = bot->GetItemCount(itemId, false);
-
-    for (uint32 slot = 0; slot < tItems->GetItemCount(); ++slot)
-    {
-        if (tItems->GetItem(slot)->item != itemId)
-            continue;
-
-        uint32 botMoney = bot->GetMoney();
-        if (botAI->HasCheat(BotCheatMask::gold))
-            bot->SetMoney(10000000);
-
-        bot->BuyItemFromVendorSlot(vendorguid, slot, itemId, 1, NULL_BAG, NULL_SLOT);
-
-        if (botAI->HasCheat(BotCheatMask::gold))
-            bot->SetMoney(botMoney);
-
-        uint32 newCount = bot->GetItemCount(itemId, false);
-        if (newCount > oldCount)
-        {
-            std::ostringstream out;
-            out << "Buying " << ChatHelper::FormatItem(proto);
-            botAI->TellMaster(out.str());
-            return true;
-        }
-
-        return false;
-    }
-
     return false;
 }
diff --git a/src/Ai/Base/Actions/BuyAction.h b/src/Ai/Base/Actions/BuyAction.h
index 86ce7ce5..4ec65ef0 100644
--- a/src/Ai/Base/Actions/BuyAction.h
+++ b/src/Ai/Base/Actions/BuyAction.h
@@ -25,7 +25,6 @@ public:
     bool Execute(Event event) override;
 
 private:
-    bool BuyItem(VendorItemData const* tItems, ObjectGuid vendorguid, ItemTemplate const* proto);
     bool TradeItem(FindItemVisitor* visitor, int8 slot);
     bool TradeItem(Item const* item, int8 slot);
 };
diff --git a/src/Ai/Base/Actions/ChatShortcutActions.cpp b/src/Ai/Base/Actions/ChatShortcutActions.cpp
index 7982deb4..f9668567 100644
--- a/src/Ai/Base/Actions/ChatShortcutActions.cpp
+++ b/src/Ai/Base/Actions/ChatShortcutActions.cpp
@@ -43,9 +43,9 @@ void PositionsResetAction::SetStayPosition(float x, float y, float z)
     posMap["stay"] = pos;
 }
 
-bool FollowChatShortcutAction::Execute(Event /*event*/)
+bool FollowChatShortcutAction::Execute(Event event)
 {
-    Player* master = GetMaster();
+    Player* master = event.getOwner() ? event.getOwner() : GetMaster();
     if (!master)
         return false;
 
@@ -115,9 +115,9 @@ bool FollowChatShortcutAction::Execute(Event /*event*/)
     return true;
 }
 
-bool StayChatShortcutAction::Execute(Event /*event*/)
+bool StayChatShortcutAction::Execute(Event event)
 {
-    Player* master = GetMaster();
+    Player* master = event.getOwner() ? event.getOwner() : GetMaster();
     if (!master)
         return false;
 
@@ -174,9 +174,9 @@ bool FleeChatShortcutAction::Execute(Event /*event*/)
     return true;
 }
 
-bool GoawayChatShortcutAction::Execute(Event /*event*/)
+bool GoawayChatShortcutAction::Execute(Event event)
 {
-    Player* master = GetMaster();
+    Player* master = event.getOwner() ? event.getOwner() : GetMaster();
     if (!master)
         return false;
 
diff --git a/src/Ai/Base/Actions/CheckMountStateAction.cpp b/src/Ai/Base/Actions/CheckMountStateAction.cpp
index e5af665b..bc2cf26e 100644
--- a/src/Ai/Base/Actions/CheckMountStateAction.cpp
+++ b/src/Ai/Base/Actions/CheckMountStateAction.cpp
@@ -312,7 +312,8 @@ bool CheckMountStateAction::TryForms(Player* master, int32 masterMountType, int3
         ((masterInShapeshiftForm == FORM_FLIGHT && botInShapeshiftForm != FORM_FLIGHT) ||
         (masterMountType == 1 && masterSpeed == 149)))
     {
-        botAI->CastSpell(SPELL_FLIGHT_FORM, bot);
+        if (!botAI->CastSpell(SPELL_FLIGHT_FORM, bot))
+            return false;
 
         // Compensate speedbuff
         bot->SetSpeed(MOVE_RUN, 2.5, true);
@@ -324,7 +325,8 @@ bool CheckMountStateAction::TryForms(Player* master, int32 masterMountType, int3
         ((masterInShapeshiftForm == FORM_FLIGHT_EPIC && botInShapeshiftForm != FORM_FLIGHT_EPIC) ||
         (masterMountType == 1 && masterSpeed >= 279)))
     {
-        botAI->CastSpell(SPELL_SWIFT_FLIGHT_FORM, bot);
+        if (!botAI->CastSpell(SPELL_SWIFT_FLIGHT_FORM, bot))
+            return false;
 
         // Compensate speedbuff
         bot->SetSpeed(MOVE_RUN, 3.8, true);
diff --git a/src/Ai/Base/Actions/FollowActions.cpp b/src/Ai/Base/Actions/FollowActions.cpp
index 21937606..f5c8a86f 100644
--- a/src/Ai/Base/Actions/FollowActions.cpp
+++ b/src/Ai/Base/Actions/FollowActions.cpp
@@ -103,7 +103,9 @@ bool FollowAction::Execute(Event /*event*/)
     std::string const target = formation->GetTargetName();
 
     // Transport handling for moving transports only (boats/zeppelins).
-    Player* master = botAI->GetMaster();
+    Player* master = botAI->GetBattlegroundCommandTarget();
+    if (!master)
+        master = botAI->GetMaster();
     if (master && master->IsInWorld() && bot->IsInWorld() && bot->GetMapId() == master->GetMapId())
     {
         Map* map = master->GetMap();
@@ -210,6 +212,9 @@ bool FollowAction::Execute(Event /*event*/)
     }
     // end unified transport handling
 
+    if (Player* commandTarget = botAI->GetBattlegroundCommandTarget())
+        return Follow(commandTarget);
+
     bool moved = false;
     if (!target.empty())
     {
@@ -250,6 +255,9 @@ bool FollowAction::isUseful()
     if (bot->GetCurrentSpell(CURRENT_CHANNELED_SPELL) != nullptr)
         return false;
 
+    if (Player* commandTarget = botAI->GetBattlegroundCommandTarget())
+        return ServerFacade::instance().IsDistanceGreaterThan(bot->GetDistance(commandTarget), sPlayerbotAIConfig.followDistance);
+
     Formation* formation = AI_VALUE(Formation*, "formation");
     if (!formation)
         return false;
diff --git a/src/Ai/Base/Actions/GenericSpellActions.cpp b/src/Ai/Base/Actions/GenericSpellActions.cpp
index 2d78e443..9e1c71e7 100644
--- a/src/Ai/Base/Actions/GenericSpellActions.cpp
+++ b/src/Ai/Base/Actions/GenericSpellActions.cpp
@@ -26,6 +26,8 @@ using ai::spell::HasSpellOrCategoryCooldown;
 
 namespace
 {
+    constexpr uint32 SPELL_SHOOT_WAND = 5019;
+
     std::unordered_set<uint32> const& GetMixedTriggerTrinketSpellIds()
     {
         static std::unordered_set<uint32> const mixedTriggerSpellIds = []()
@@ -427,6 +429,9 @@ CastShootAction::CastShootAction(
                 spell = "throw";
                 shootSpellId = 2764;
                 break;
+            case ITEM_SUBCLASS_WEAPON_WAND:
+                shootSpellId = SPELL_SHOOT_WAND;
+                break;
         }
     }
 }
@@ -528,6 +533,16 @@ bool UseTrinketAction::UseTrinket(Item* item)
     if (bot->CanUseItem(item) != EQUIP_ERR_OK || bot->IsNonMeleeSpellCast(true))
         return false;
 
+    if (item && item->GetEntry() == 18706)
+    {
+        if (!bot->IsInCombat())
+            return false;
+
+        uint8 const healthPct = AI_VALUE2(uint8, "health", "self target");
+        if (healthPct > 30)
+            return false;
+    }
+
     uint8 bagIndex = item->GetBagSlot();
     uint8 slot = item->GetSlot();
     uint8 cast_count = 1;
diff --git a/src/Ai/Base/Actions/GenericSpellActions.h b/src/Ai/Base/Actions/GenericSpellActions.h
index a5f802a4..5bbdd792 100644
--- a/src/Ai/Base/Actions/GenericSpellActions.h
+++ b/src/Ai/Base/Actions/GenericSpellActions.h
@@ -208,6 +208,7 @@ class ResurrectPartyMemberAction : public CastSpellAction
 public:
     ResurrectPartyMemberAction(PlayerbotAI* botAI, std::string const spell) : CastSpellAction(botAI, spell) {}
 
+    bool isUseful() override { return false; }
     std::string const GetTargetName() override { return "party member to resurrect"; }
     std::vector<NextAction> getPrerequisites() override
     {
diff --git a/src/Ai/Base/Actions/GuildCreateActions.cpp b/src/Ai/Base/Actions/GuildCreateActions.cpp
index 59696ece..2acfd44a 100644
--- a/src/Ai/Base/Actions/GuildCreateActions.cpp
+++ b/src/Ai/Base/Actions/GuildCreateActions.cpp
@@ -16,48 +16,6 @@
 
 bool BuyPetitionAction::Execute(Event /*event*/)
 {
-    GuidVector vendors = botAI->GetAiObjectContext()->GetValue<GuidVector>("nearest npcs")->Get();
-    for (GuidVector::iterator i = vendors.begin(); i != vendors.end(); ++i)
-    {
-        ObjectGuid vendorguid = *i;
-        Creature* pCreature = bot->GetNPCIfCanInteractWith(vendorguid, UNIT_NPC_FLAG_PETITIONER);
-        if (!pCreature)
-            continue;
-
-        std::string const guildName = RandomPlayerbotFactory::CreateRandomGuildName();
-        if (guildName.empty())
-            continue;
-
-        WorldPacket data(CMSG_PETITION_BUY);
-
-        data << pCreature->GetGUID();
-        data << uint32(0);
-        data << uint64(0);
-        data << guildName.c_str();
-        data << std::string("");
-        data << uint32(0);
-        data << uint32(0);
-        data << uint32(0);
-        data << uint32(0);
-        data << uint32(0);
-        data << uint32(0);
-        data << uint32(0);
-        data << uint16(0);
-        data << uint32(0);
-        data << uint32(0);
-        data << uint32(0);
-
-        for (uint8 i = 0; i < 10; ++i)
-            data << std::string("");
-
-        data << uint32(0);  // index
-        data << uint32(0);
-
-        bot->GetSession()->HandlePetitionBuyOpcode(data);
-
-        return true;
-    }
-
     return false;
 }
 
diff --git a/src/Ai/Base/Actions/QuestAction.cpp b/src/Ai/Base/Actions/QuestAction.cpp
index 769cd133..d548a56e 100644
--- a/src/Ai/Base/Actions/QuestAction.cpp
+++ b/src/Ai/Base/Actions/QuestAction.cpp
@@ -213,53 +213,8 @@ bool QuestAction::ProcessQuests(WorldObject* questGiver)
     return true;
 }
 
-bool QuestAction::AcceptQuest(Quest const* quest, ObjectGuid questGiver)
+bool QuestAction::AcceptQuest(Quest const*, ObjectGuid)
 {
-    std::ostringstream out;
-
-    uint32 questId = quest->GetQuestId();
-
-    if (bot->GetQuestStatus(questId) == QUEST_STATUS_COMPLETE)
-        out << "Already completed";
-    else if (!bot->CanTakeQuest(quest, false))
-    {
-        if (!bot->SatisfyQuestStatus(quest, false))
-            out << "Already on";
-        else
-            out << "Can't take";
-    }
-    else if (!bot->SatisfyQuestLog(false))
-        out << "Quest log is full";
-    else if (!bot->CanAddQuest(quest, false))
-        out << "Bags are full";
-    else
-    {
-        WorldPacket p(CMSG_QUESTGIVER_ACCEPT_QUEST);
-        uint32 unk1 = 0;
-        p << questGiver << questId << unk1;
-        p.rpos(0);
-        bot->GetSession()->HandleQuestgiverAcceptQuestOpcode(p);
-
-        if (bot->GetQuestStatus(questId) == QUEST_STATUS_NONE && sPlayerbotAIConfig.syncQuestWithPlayer)
-        {
-            Object* pObject = ObjectAccessor::GetObjectByTypeMask(*bot, questGiver,
-                                                                  TYPEMASK_UNIT | TYPEMASK_GAMEOBJECT | TYPEMASK_ITEM);
-            bot->AddQuest(quest, pObject);
-        }
-
-        if (bot->GetQuestStatus(questId) != QUEST_STATUS_NONE && bot->GetQuestStatus(questId) != QUEST_STATUS_REWARDED)
-        {
-            BroadcastHelper::BroadcastQuestAccepted(botAI, bot, quest);
-            out << "Accepted " << chat->FormatQuest(quest);
-            botAI->TellMaster(out);
-            return true;
-        }
-        out << "Cannot accept";
-    }
-
-    out << " " << chat->FormatQuest(quest);
-    botAI->TellMaster(out);
-
     return false;
 }
 
diff --git a/src/Ai/Base/Actions/QuestConfirmAcceptAction.cpp b/src/Ai/Base/Actions/QuestConfirmAcceptAction.cpp
index 4197f53e..64776e0a 100644
--- a/src/Ai/Base/Actions/QuestConfirmAcceptAction.cpp
+++ b/src/Ai/Base/Actions/QuestConfirmAcceptAction.cpp
@@ -3,24 +3,7 @@
 #include "QuestPackets.h"
 #include "WorldPacket.h"
 
-bool QuestConfirmAcceptAction::Execute(Event event)
+bool QuestConfirmAcceptAction::Execute(Event)
 {
-    WorldPacket packet(event.getPacket());
-    uint32 questId;
-    packet >> questId;
-
-    WorldPacket sendPacket(CMSG_QUEST_CONFIRM_ACCEPT);
-    sendPacket << questId;
-    Quest const* quest = sObjectMgr->GetQuestTemplate(questId);
-    if (!quest || !bot->CanAddQuest(quest, true))
-    {
-        return false;
-    }
-    std::ostringstream out;
-    out << "Quest: " << chat->FormatQuest(quest) << " confirm accept";
-    botAI->TellMaster(out);
-    WorldPackets::Quest::QuestConfirmAcceptClient confirmAccept(std::move(sendPacket));
-    confirmAccept.Read();
-    bot->GetSession()->HandleQuestConfirmAccept(confirmAccept);
-    return true;
+    return false;
 }
diff --git a/src/Ai/Base/Actions/SayAction.cpp b/src/Ai/Base/Actions/SayAction.cpp
index 1e8af760..930f422a 100644
--- a/src/Ai/Base/Actions/SayAction.cpp
+++ b/src/Ai/Base/Actions/SayAction.cpp
@@ -144,6 +144,9 @@ bool SayAction::Execute(Event /*event*/)
 
 bool SayAction::isUseful()
 {
+    if (!sPlayerbotAIConfig.randomBotTalk)
+        return false;
+
     if (!botAI->AllowActivity())
         return false;
 
diff --git a/src/Ai/Base/Actions/TravelAction.cpp b/src/Ai/Base/Actions/TravelAction.cpp
index feb5b973..c547b9ca 100644
--- a/src/Ai/Base/Actions/TravelAction.cpp
+++ b/src/Ai/Base/Actions/TravelAction.cpp
@@ -76,30 +76,6 @@ bool MoveToDarkPortalAction::Execute(Event /*event*/)
         {
             if (!bot->IsInCombat())
             {
-                if (bot->GetTeamId() == TEAM_ALLIANCE)
-                {
-                    Quest const* quest = sObjectMgr->GetQuestTemplate(10119);
-                    CreatureData const* creatureData = sRandomPlayerbotMgr.GetCreatureDataByEntry(16841);
-                    if (quest && creatureData)
-                    {
-                        auto creatureBounds =
-                            bot->GetMap()->GetCreatureBySpawnIdStore().equal_range(creatureData->spawnId);
-                        if (creatureBounds.first != creatureBounds.second)
-                            bot->AddQuest(quest, creatureBounds.first->second);
-                    }
-                }
-                else
-                {
-                    Quest const* quest = sObjectMgr->GetQuestTemplate(9407);
-                    CreatureData const* creatureData = sRandomPlayerbotMgr.GetCreatureDataByEntry(19254);
-                    if (quest && creatureData)
-                    {
-                        auto creatureBounds =
-                            bot->GetMap()->GetCreatureBySpawnIdStore().equal_range(creatureData->spawnId);
-                        if (creatureBounds.first != creatureBounds.second)
-                            bot->AddQuest(quest, creatureBounds.first->second);
-                    }
-                }
             }
 
             return MoveTo(0, -11815.1f, -3190.39f, -30.7338f, false, true);
diff --git a/src/Ai/Base/Actions/UseItemAction.cpp b/src/Ai/Base/Actions/UseItemAction.cpp
index 403dbc05..e58e5959 100644
--- a/src/Ai/Base/Actions/UseItemAction.cpp
+++ b/src/Ai/Base/Actions/UseItemAction.cpp
@@ -168,20 +168,8 @@ bool UseItemAction::UseItem(Item* item, ObjectGuid goGuid, Item* itemTarget, Uni
         targetSelected = true;
     }
 
-    if (uint32 questid = item->GetTemplate()->StartQuest)
-    {
-        if (Quest const* qInfo = sObjectMgr->GetQuestTemplate(questid))
-        {
-            WorldPacket packet(CMSG_QUESTGIVER_ACCEPT_QUEST, 8 + 4 + 4);
-            packet << item_guid;
-            packet << questid;
-            packet << uint32(0);
-            bot->GetSession()->HandleQuestgiverAcceptQuestOpcode(packet);
-
-            botAI->TellMasterNoFacing("Got quest " + chat->FormatQuest(qInfo));
-            return true;
-        }
-    }
+    if (item->GetTemplate()->StartQuest)
+        return false;
 
     bot->ClearUnitState(UNIT_STATE_CHASE);
     bot->ClearUnitState(UNIT_STATE_FOLLOW);
diff --git a/src/Ai/Base/Strategy/BattlegroundStrategy.cpp b/src/Ai/Base/Strategy/BattlegroundStrategy.cpp
index f414e752..b2b981e4 100644
--- a/src/Ai/Base/Strategy/BattlegroundStrategy.cpp
+++ b/src/Ai/Base/Strategy/BattlegroundStrategy.cpp
@@ -9,7 +9,7 @@
 
 void BGStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 {
-    triggers.push_back(new TriggerNode("often", { NextAction("bg join", relevance)}));
+    triggers.push_back(new TriggerNode("often", { NextAction("bg join", 101.0f)}));
     triggers.push_back(new TriggerNode("bg invite active", { NextAction("bg status check", relevance)}));
     triggers.push_back(new TriggerNode("timer", { NextAction("bg strategy check", relevance)}));
 }
diff --git a/src/Ai/Base/Value/CcTargetValue.cpp b/src/Ai/Base/Value/CcTargetValue.cpp
index 4903a59f..a52ba2c3 100644
--- a/src/Ai/Base/Value/CcTargetValue.cpp
+++ b/src/Ai/Base/Value/CcTargetValue.cpp
@@ -7,9 +7,37 @@
 
 #include "Action.h"
 #include "AiObjectContext.h"
+#include "GameTime.h"
 #include "Group.h"
 #include "PlayerbotAI.h"
 #include "ServerFacade.h"
+#include <unordered_map>
+
+namespace
+{
+    std::unordered_map<ObjectGuid, std::unordered_map<ObjectGuid, uint32>> s_lastPolymorphTimes;
+}
+
+bool IsPolymorphOnCooldownForTarget(ObjectGuid botGuid, ObjectGuid targetGuid)
+{
+    uint32 now = GameTime::GetGameTimeMS().count();
+    auto botIt = s_lastPolymorphTimes.find(botGuid);
+    if (botIt != s_lastPolymorphTimes.end())
+    {
+        auto targetIt = botIt->second.find(targetGuid);
+        if (targetIt != botIt->second.end())
+        {
+            if (now - targetIt->second < 30000)
+                return true;
+        }
+    }
+    return false;
+}
+
+void SetPolymorphTargetCooldown(ObjectGuid botGuid, ObjectGuid targetGuid)
+{
+    s_lastPolymorphTimes[botGuid][targetGuid] = GameTime::GetGameTimeMS().count();
+}
 
 class FindTargetForCcStrategy : public FindTargetStrategy
 {
@@ -35,6 +63,43 @@ public:
         if (*botAI->GetAiObjectContext()->GetValue<Unit*>("current target") == creature)
             return;
 
+        if (botAI->HasAura(spell, creature) ||
+            creature->HasAuraType(SPELL_AURA_MOD_CONFUSE) ||
+            creature->HasAuraType(SPELL_AURA_MOD_STUN) ||
+            creature->HasAuraType(SPELL_AURA_MOD_FEAR) ||
+            botAI->HasAura("cyclone", creature) ||
+            botAI->HasAura("freezing trap", creature) ||
+            creature->IsPolymorphed())
+            return;
+
+        if (spell == "polymorph")
+        {
+            // Do not sheep targets with <= 70% HP
+            if (creature->GetHealthPct() <= 70)
+                return;
+
+            // Do not sheep shapeshifted druids (immune)
+            if (creature->GetShapeshiftForm() != FORM_NONE)
+                return;
+
+            // Do not sheep DR immune targets
+            if (creature->GetDiminishing(DIMINISHING_DISORIENT) >= DIMINISHING_LEVEL_IMMUNE)
+                return;
+
+            // Do not sheep targets with active DoTs (breaks CC immediately)
+            if (creature->HasAuraType(SPELL_AURA_PERIODIC_DAMAGE) ||
+                creature->HasAuraType(SPELL_AURA_PERIODIC_DAMAGE_PERCENT) ||
+                creature->HasAuraType(SPELL_AURA_PERIODIC_LEECH))
+                return;
+
+            // 30s cooldown per target
+            if (IsPolymorphOnCooldownForTarget(bot->GetGUID(), creature->GetGUID()))
+                return;
+
+            result = creature;
+            return;
+        }
+
         uint8 health = static_cast<uint8>(creature->GetHealthPct());
         if (health < sPlayerbotAIConfig.mediumHealth)
             return;
@@ -92,5 +157,22 @@ private:
 Unit* CcTargetValue::Calculate()
 {
     FindTargetForCcStrategy strategy(botAI, qualifier);
-    return FindTarget(&strategy);
+    Unit* target = FindTarget(&strategy);
+    if (target)
+        return target;
+
+    Unit* currentTarget = *botAI->GetAiObjectContext()->GetValue<Unit*>("current target");
+    GuidVector targets = botAI->GetAiObjectContext()->GetValue<GuidVector>("possible targets")->Get();
+    for (ObjectGuid const guid : targets)
+    {
+        Unit* unit = botAI->GetUnit(guid);
+        if (!unit || unit == currentTarget)
+            continue;
+
+        strategy.CheckAttacker(unit, nullptr);
+        if (strategy.GetResult())
+            return strategy.GetResult();
+    }
+
+    return nullptr;
 }
diff --git a/src/Ai/Base/Value/CcTargetValue.h b/src/Ai/Base/Value/CcTargetValue.h
index c487523c..e437a3a2 100644
--- a/src/Ai/Base/Value/CcTargetValue.h
+++ b/src/Ai/Base/Value/CcTargetValue.h
@@ -12,6 +12,9 @@
 class PlayerbotAI;
 class Unit;
 
+bool IsPolymorphOnCooldownForTarget(ObjectGuid botGuid, ObjectGuid targetGuid);
+void SetPolymorphTargetCooldown(ObjectGuid botGuid, ObjectGuid targetGuid);
+
 class CcTargetValue : public TargetValue, public Qualified
 {
 public:
diff --git a/src/Ai/Base/Value/InvalidTargetValue.cpp b/src/Ai/Base/Value/InvalidTargetValue.cpp
index 50f7a386..c3c938d3 100644
--- a/src/Ai/Base/Value/InvalidTargetValue.cpp
+++ b/src/Ai/Base/Value/InvalidTargetValue.cpp
@@ -6,6 +6,8 @@
 #include "InvalidTargetValue.h"
 
 #include "AttackersValue.h"
+#include "Item.h"
+#include "ItemTemplate.h"
 #include "Playerbots.h"
 #include "Unit.h"
 
@@ -18,10 +20,15 @@ bool InvalidTargetValue::Calculate()
 
     if (target && qualifier == "current target")
     {
+        Item* ranged = bot->GetItemByPos(INVENTORY_SLOT_BAG_0, EQUIPMENT_SLOT_RANGED);
+        bool const priestCanWandFearedTarget = bot->getClass() == CLASS_PRIEST && target->HasFearAura() && ranged &&
+                                                ranged->GetTemplate()->SubClass == ITEM_SUBCLASS_WEAPON_WAND;
+
         return target->GetMapId() != bot->GetMapId() || target->HasUnitFlag(UNIT_FLAG_NOT_SELECTABLE) ||
                target->HasUnitFlag(UNIT_FLAG_NON_ATTACKABLE) || target->HasUnitFlag(UNIT_FLAG_NON_ATTACKABLE_2) ||
                !target->IsVisible() || !target->IsAlive() || target->IsPolymorphed() || target->IsCharmed() ||
-               target->HasFearAura() || target->HasUnitState(UNIT_STATE_ISOLATED) || target->IsFriendlyTo(bot) ||
+               (target->HasFearAura() && !priestCanWandFearedTarget) ||
+               target->HasUnitState(UNIT_STATE_ISOLATED) || target->IsFriendlyTo(bot) ||
                !AttackersValue::IsValidTarget(target, bot);
     }
 
diff --git a/src/Ai/Class/Druid/Strategy/GenericDruidNonCombatStrategy.cpp b/src/Ai/Class/Druid/Strategy/GenericDruidNonCombatStrategy.cpp
index a376aea0..861c5629 100644
--- a/src/Ai/Class/Druid/Strategy/GenericDruidNonCombatStrategy.cpp
+++ b/src/Ai/Class/Druid/Strategy/GenericDruidNonCombatStrategy.cpp
@@ -174,6 +174,7 @@ void GenericDruidNonCombatStrategy::InitTriggers(std::vector<TriggerNode*>& trig
                        }));
 
     triggers.push_back(new TriggerNode("aquatic form", { NextAction("aquatic form", 10.0f) }));
+    triggers.push_back(new TriggerNode("player has flag", { NextAction("travel form", ACTION_EMERGENCY + 3) }));
 
     int specTab = AiFactory::GetPlayerSpecTab(botAI->GetBot());
     if (specTab == DRUID_TAB_BALANCE || specTab == DRUID_TAB_RESTORATION)
diff --git a/src/Ai/Class/Druid/Strategy/GenericDruidStrategy.cpp b/src/Ai/Class/Druid/Strategy/GenericDruidStrategy.cpp
index 5895ac02..04be2831 100644
--- a/src/Ai/Class/Druid/Strategy/GenericDruidStrategy.cpp
+++ b/src/Ai/Class/Druid/Strategy/GenericDruidStrategy.cpp
@@ -78,6 +78,7 @@ void GenericDruidStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 
     triggers.push_back(new TriggerNode("being attacked",
                                        { NextAction("nature's grasp", 39.0f) }));
+    triggers.push_back(new TriggerNode("player has flag", { NextAction("travel form", ACTION_EMERGENCY + 3) }));
 }
 
 void DruidCureStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
diff --git a/src/Ai/Class/Mage/MageActions.h b/src/Ai/Class/Mage/MageActions.h
index 48ce5079..84249a6b 100644
--- a/src/Ai/Class/Mage/MageActions.h
+++ b/src/Ai/Class/Mage/MageActions.h
@@ -6,6 +6,7 @@
 #ifndef PLAYERBOTS_MAGEACTIONS_H
 #define PLAYERBOTS_MAGEACTIONS_H
 
+#include "CcTargetValue.h"
 #include "GenericSpellActions.h"
 #include "SharedDefines.h"
 #include "UseItemAction.h"
@@ -222,6 +223,14 @@ class CastPolymorphAction : public CastCrowdControlSpellAction
 {
 public:
     CastPolymorphAction(PlayerbotAI* botAI) : CastCrowdControlSpellAction(botAI, "polymorph") {}
+
+    bool Execute(Event event) override
+    {
+        Unit* target = GetTarget();
+        if (target)
+            SetPolymorphTargetCooldown(bot->GetGUID(), target->GetGUID());
+        return CastCrowdControlSpellAction::Execute(event);
+    }
 };
 
 class CastSpellstealAction : public CastSpellAction
diff --git a/src/Ai/Class/Mage/Strategy/GenericMageStrategy.cpp b/src/Ai/Class/Mage/Strategy/GenericMageStrategy.cpp
index 9277cb65..db42799a 100644
--- a/src/Ai/Class/Mage/Strategy/GenericMageStrategy.cpp
+++ b/src/Ai/Class/Mage/Strategy/GenericMageStrategy.cpp
@@ -91,6 +91,8 @@ void GenericMageStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 {
     RangedCombatStrategy::InitTriggers(triggers);
 
+    triggers.push_back(new TriggerNode("polymorph", { NextAction("polymorph", ACTION_INTERRUPT + 1) }));
+
     // Threat Triggers
     triggers.push_back(new TriggerNode("high threat", { NextAction("mirror image", 60.0f) }));
     triggers.push_back(new TriggerNode("medium threat", { NextAction("invisibility", 30.0f) }));
@@ -101,6 +103,7 @@ void GenericMageStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
     triggers.push_back(new TriggerNode("fire ward", { NextAction("fire ward", 90.0f) }));
     triggers.push_back(new TriggerNode("frost ward", { NextAction("frost ward", 90.0f) }));
     triggers.push_back(new TriggerNode("enemy is close and no firestarter strategy", { NextAction("frost nova", 50.0f) }));
+    triggers.push_back(new TriggerNode("being attacked", { NextAction("frost nova", ACTION_INTERRUPT + 1) }));
     triggers.push_back(new TriggerNode("enemy too close for spell and no firestarter strategy", { NextAction("blink back", 35.0f) }));
 
     // Mana Threshold Triggers
@@ -166,8 +169,6 @@ void MageBoostStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 
 void MageCcStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 {
-    triggers.push_back(new TriggerNode("polymorph", { NextAction("polymorph", 30.0f) }));
-
     Player* bot = botAI->GetBot();
     int tab = AiFactory::GetPlayerSpecTab(bot);
     if (tab == MAGE_TAB_FIRE)
diff --git a/src/Ai/Class/Priest/PriestActions.h b/src/Ai/Class/Priest/PriestActions.h
index fb57eaec..438339d8 100644
--- a/src/Ai/Class/Priest/PriestActions.h
+++ b/src/Ai/Class/Priest/PriestActions.h
@@ -95,7 +95,7 @@ public:
 };
 AOE_HEAL_ACTION(CastLightwellAction, "lightwell", 15.0f, HealingManaEfficiency::MEDIUM);
 
-SPELL_ACTION(CastSmiteAction, "smite");
+SPELL_ACTION_U(CastSmiteAction, "smite", AI_VALUE2(uint8, "mana", "self target") > 70);
 SPELL_ACTION(CastHolyNovaAction, "holy nova");
 
 RESS_ACTION(CastResurrectionAction, "resurrection");
diff --git a/src/Ai/Class/Priest/Strategy/GenericPriestStrategy.cpp b/src/Ai/Class/Priest/Strategy/GenericPriestStrategy.cpp
index 60da1343..ba5c6844 100644
--- a/src/Ai/Class/Priest/Strategy/GenericPriestStrategy.cpp
+++ b/src/Ai/Class/Priest/Strategy/GenericPriestStrategy.cpp
@@ -38,6 +38,8 @@ void GenericPriestStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 
     triggers.push_back(new TriggerNode("enemy too close for spell",
                                        { NextAction("flee", ACTION_MOVE + 9) }));
+    triggers.push_back(new TriggerNode("enemy is close",
+                                       { NextAction("psychic scream", ACTION_INTERRUPT + 1) }));
     triggers.push_back(new TriggerNode("often", { NextAction("apply oil", 1.0f) }));
     triggers.push_back(new TriggerNode("being attacked",
         { NextAction("power word: shield", ACTION_HIGH + 1) }));
@@ -81,9 +83,9 @@ void PriestHealerDpsStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
                         {
                             NextAction("shadow word: pain", ACTION_DEFAULT + 0.5f),
                             NextAction("holy fire", ACTION_DEFAULT + 0.4f),
-                            NextAction("smite", ACTION_DEFAULT + 0.3f),
-                            NextAction("mind blast", ACTION_DEFAULT + 0.2f),
-                            NextAction("shoot", ACTION_DEFAULT) }));
+                            NextAction("shoot", ACTION_DEFAULT + 0.3f),
+                            NextAction("smite", ACTION_DEFAULT + 0.2f),
+                            NextAction("mind blast", ACTION_DEFAULT + 0.1f) }));
 
     triggers.push_back(
         new TriggerNode("medium aoe and healer should attack",
diff --git a/src/Ai/Class/Priest/Strategy/HolyPriestStrategy.cpp b/src/Ai/Class/Priest/Strategy/HolyPriestStrategy.cpp
index 399f21c6..8d84d153 100644
--- a/src/Ai/Class/Priest/Strategy/HolyPriestStrategy.cpp
+++ b/src/Ai/Class/Priest/Strategy/HolyPriestStrategy.cpp
@@ -32,6 +32,7 @@ HolyPriestStrategy::HolyPriestStrategy(PlayerbotAI* botAI) : HealPriestStrategy(
 std::vector<NextAction> HolyPriestStrategy::getDefaultActions()
 {
     return {
+        NextAction("shoot", ACTION_DEFAULT + 0.3f),
         NextAction("smite", ACTION_DEFAULT + 0.2f),
         NextAction("mana burn", ACTION_DEFAULT + 0.1f),
         NextAction("starshards", ACTION_DEFAULT)
diff --git a/src/Ai/Class/Rogue/Strategy/GenericRogueNonCombatStrategy.cpp b/src/Ai/Class/Rogue/Strategy/GenericRogueNonCombatStrategy.cpp
index 88838f2d..243dcdf7 100644
--- a/src/Ai/Class/Rogue/Strategy/GenericRogueNonCombatStrategy.cpp
+++ b/src/Ai/Class/Rogue/Strategy/GenericRogueNonCombatStrategy.cpp
@@ -7,28 +7,7 @@
 
 #include "Playerbots.h"
 
-class GenericRogueNonCombatStrategyActionNodeFactory : public NamedObjectFactory<ActionNode>
-{
-public:
-    GenericRogueNonCombatStrategyActionNodeFactory()
-    {
-        creators["use deadly poison on off hand"] = &use_deadly_poison_on_off_hand;
-    }
-
-private:
-    static ActionNode* use_deadly_poison_on_off_hand([[maybe_unused]] PlayerbotAI* botAI)
-    {
-        return new ActionNode("use deadly poison on off hand",
-                              /*P*/ {},
-                              /*A*/ { NextAction("use instant poison on off hand") },
-                              /*C*/ {});
-    }
-};
-
-GenericRogueNonCombatStrategy::GenericRogueNonCombatStrategy(PlayerbotAI* botAI) : NonCombatStrategy(botAI)
-{
-    actionNodeFactories.Add(new GenericRogueNonCombatStrategyActionNodeFactory());
-}
+GenericRogueNonCombatStrategy::GenericRogueNonCombatStrategy(PlayerbotAI* botAI) : NonCombatStrategy(botAI) {}
 
 void GenericRogueNonCombatStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 {
@@ -38,13 +17,5 @@ void GenericRogueNonCombatStrategy::InitTriggers(std::vector<TriggerNode*>& trig
                                        { NextAction("sprint", ACTION_EMERGENCY + 1) }));
     triggers.push_back(new TriggerNode("enemy flagcarrier near",
                                        { NextAction("sprint", ACTION_EMERGENCY + 2) }));
-    triggers.push_back(
-        new TriggerNode("main hand weapon no enchant",
-                        { NextAction("use instant poison on main hand", 20.0f) }));
-
-    triggers.push_back(
-        new TriggerNode("off hand weapon no enchant",
-                        { NextAction("use deadly poison on off hand", 19.0f) }));
-
     triggers.push_back(new TriggerNode("often", { NextAction("unstealth", 30.0f) }));
 }
diff --git a/src/Ai/Class/Warlock/Strategy/TankWarlockStrategy.cpp b/src/Ai/Class/Warlock/Strategy/TankWarlockStrategy.cpp
index 6e3c6cdc..2babef13 100644
--- a/src/Ai/Class/Warlock/Strategy/TankWarlockStrategy.cpp
+++ b/src/Ai/Class/Warlock/Strategy/TankWarlockStrategy.cpp
@@ -14,7 +14,8 @@ std::vector<NextAction> TankWarlockStrategy::getDefaultActions()
 {
     return {
         NextAction("shadow ward", 27.5f),
-        NextAction("searing pain", 27.0f)
+        NextAction("searing pain", 27.0f),
+        NextAction("shoot", 26.9f)
     };
 }
 
diff --git a/src/Ai/Class/Warrior/Strategy/ArmsWarriorStrategy.cpp b/src/Ai/Class/Warrior/Strategy/ArmsWarriorStrategy.cpp
index 4addfa05..6f88023f 100644
--- a/src/Ai/Class/Warrior/Strategy/ArmsWarriorStrategy.cpp
+++ b/src/Ai/Class/Warrior/Strategy/ArmsWarriorStrategy.cpp
@@ -77,8 +77,6 @@ ArmsWarriorStrategy::ArmsWarriorStrategy(PlayerbotAI* botAI) : GenericWarriorStr
 std::vector<NextAction> ArmsWarriorStrategy::getDefaultActions()
 {
     return {
-        NextAction("bladestorm", ACTION_DEFAULT + 0.2f),
-        NextAction("mortal strike", ACTION_DEFAULT + 0.1f),
         NextAction("sunder armor", ACTION_DEFAULT + 0.05f),
         NextAction("melee", ACTION_DEFAULT)
     };
@@ -96,6 +94,15 @@ void ArmsWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
         )
     );
 
+    triggers.push_back(
+        new TriggerNode(
+            "disarm",
+            {
+                NextAction("disarm", ACTION_HIGH + 11)
+            }
+        )
+    );
+
     triggers.push_back(
         new TriggerNode(
             "battle stance",
@@ -107,81 +114,81 @@ void ArmsWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 
     triggers.push_back(
         new TriggerNode(
-            "battle shout",
+            "hamstring",
             {
-                NextAction("battle shout", ACTION_HIGH + 9)
+                NextAction("hamstring", ACTION_HIGH + 9)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "rend",
+            "battle shout",
             {
-                NextAction("rend", ACTION_HIGH + 8)
+                NextAction("battle shout", ACTION_HIGH + 8)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "rend on attacker",
+            "rend",
             {
-                NextAction("rend on attacker", ACTION_HIGH + 8)
+                NextAction("rend", ACTION_HIGH + 7)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "mortal strike",
+            "rend on attacker",
             {
-                NextAction("mortal strike", ACTION_HIGH + 3)
+                NextAction("rend on attacker", ACTION_HIGH + 7)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "target critical health",
+            "overpower",
             {
-                NextAction("execute", ACTION_HIGH + 5)
+                NextAction("overpower", ACTION_HIGH + 4)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "sudden death",
+            "taste for blood",
             {
-                NextAction("execute", ACTION_HIGH + 5)
+                NextAction("overpower", ACTION_HIGH + 4)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "hamstring",
+            "shield bash on enemy healer",
             {
-                NextAction("piercing howl", ACTION_HIGH)
+                NextAction("shield bash on enemy healer", ACTION_INTERRUPT + 2)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "overpower",
+            "shield bash",
             {
-                NextAction("overpower", ACTION_HIGH + 4)
+                NextAction("shield bash", ACTION_INTERRUPT + 1)
             }
         )
     );
 
     triggers.push_back(
         new TriggerNode(
-            "taste for blood",
+            "shield block",
             {
-                NextAction("overpower", ACTION_HIGH + 4)
+                NextAction("shield block", ACTION_HIGH + 3)
             }
         )
     );
diff --git a/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.cpp b/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.cpp
index 18781ef3..bb0393da 100644
--- a/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.cpp
+++ b/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.cpp
@@ -27,6 +27,7 @@ private:
 GenericWarriorStrategy::GenericWarriorStrategy(PlayerbotAI* botAI) : CombatStrategy(botAI)
 {
     actionNodeFactories.Add(new GenericWarriorStrategyActionNodeFactory());
+    actionNodeFactories.Add(new WarriorStanceRequirementActionNodeFactory());
 }
 
 void GenericWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
@@ -59,7 +60,6 @@ void WarrirorAoeStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
 {
     triggers.push_back(new TriggerNode(
         "light aoe", { NextAction("sweeping strikes", ACTION_HIGH + 7),
-                                       NextAction("bladestorm", ACTION_HIGH + 6),
                                        NextAction("thunder clap", ACTION_HIGH + 5),
                                        NextAction("shockwave", ACTION_HIGH + 4),
                                        NextAction("demoralizing shout without life time check", ACTION_HIGH + 1),
diff --git a/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.h b/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.h
index efd7eba2..27f5d66c 100644
--- a/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.h
+++ b/src/Ai/Class/Warrior/Strategy/GenericWarriorStrategy.h
@@ -22,6 +22,7 @@ public:
         creators["overpower"] = &overpower;
         creators["retaliation"] = &retaliation;
         creators["shattering throw"] = &shattering_throw;
+        creators["hamstring"] = &hamstring;
 
         // temp
         creators["mortal strike"] = &mortal_strike;
@@ -159,7 +160,7 @@ private:
         return new ActionNode(
             "disarm",
             /*P*/ { NextAction("defensive stance") },
-            /*A*/ {},
+            /*A*/ { NextAction("bloodrage") },
             /*C*/ {}
         );
     }
@@ -213,6 +214,16 @@ private:
             /*C*/ {}
         );
     }
+
+    static ActionNode* hamstring([[maybe_unused]] PlayerbotAI* botAI)
+    {
+        return new ActionNode(
+            "hamstring",
+            /*P*/ { NextAction("battle stance") },
+            /*A*/ {},
+            /*C*/ {}
+        );
+    }
 };
 
 class GenericWarriorStrategy : public CombatStrategy
diff --git a/src/Ai/Class/Warrior/Strategy/TankWarriorStrategy.cpp b/src/Ai/Class/Warrior/Strategy/TankWarriorStrategy.cpp
index 727039e2..716eb9b1 100644
--- a/src/Ai/Class/Warrior/Strategy/TankWarriorStrategy.cpp
+++ b/src/Ai/Class/Warrior/Strategy/TankWarriorStrategy.cpp
@@ -12,8 +12,13 @@ public:
     {
         creators["charge"] = &charge;
         creators["sunder armor"] = &sunder_armor;
+        creators["hamstring"] = &hamstring;
+        creators["shield bash"] = &shield_bash;
+        creators["shield bash on enemy healer"] = &shield_bash_on_enemy_healer;
+        creators["revenge"] = &revenge;
+        creators["shield block"] = &shield_block;
+        creators["disarm"] = &disarm;
         creators["commanding shout"] = &commanding_shout;
-        creators["devastate"] = &devastate;
         creators["last stand"] = &last_stand;
         creators["heroic throw on snare target"] = &heroic_throw_on_snare_target;
         creators["heroic throw taunt"] = &heroic_throw_taunt;
@@ -27,7 +32,7 @@ private:
         return new ActionNode(
             "heroic throw",
             /*P*/ {},
-            /*A*/ { NextAction("shield slam") },
+            /*A*/ { NextAction("melee") },
             /*C*/ {}
         );
     }
@@ -52,12 +57,62 @@ private:
         );
     }
 
-    static ActionNode* devastate(PlayerbotAI* /*botAI*/)
+    static ActionNode* hamstring(PlayerbotAI* /*botAI*/)
     {
         return new ActionNode(
-            "devastate",
-            /*P*/ {},
-            /*A*/ { NextAction("sunder armor") },
+            "hamstring",
+            /*P*/ { NextAction("battle stance") },
+            /*A*/ {},
+            /*C*/ {}
+        );
+    }
+
+    static ActionNode* shield_bash(PlayerbotAI* /*botAI*/)
+    {
+        return new ActionNode(
+            "shield bash",
+            /*P*/ { NextAction("defensive stance") },
+            /*A*/ {},
+            /*C*/ {}
+        );
+    }
+
+    static ActionNode* shield_bash_on_enemy_healer(PlayerbotAI* /*botAI*/)
+    {
+        return new ActionNode(
+            "shield bash on enemy healer",
+            /*P*/ { NextAction("defensive stance") },
+            /*A*/ {},
+            /*C*/ {}
+        );
+    }
+
+    static ActionNode* revenge(PlayerbotAI* /*botAI*/)
+    {
+        return new ActionNode(
+            "revenge",
+            /*P*/ { NextAction("defensive stance") },
+            /*A*/ {},
+            /*C*/ {}
+        );
+    }
+
+    static ActionNode* shield_block(PlayerbotAI* /*botAI*/)
+    {
+        return new ActionNode(
+            "shield block",
+            /*P*/ { NextAction("defensive stance") },
+            /*A*/ {},
+            /*C*/ {}
+        );
+    }
+
+    static ActionNode* disarm(PlayerbotAI* /*botAI*/)
+    {
+        return new ActionNode(
+            "disarm",
+            /*P*/ { NextAction("defensive stance") },
+            /*A*/ {},
             /*C*/ {}
         );
     }
@@ -86,7 +141,7 @@ private:
     {
         return new ActionNode(
             "charge",
-            /*P*/ {},
+            /*P*/ { NextAction("battle stance") },
             /*A*/ { NextAction("reach melee") },
             /*C*/ {}
         );
@@ -111,9 +166,7 @@ TankWarriorStrategy::TankWarriorStrategy(PlayerbotAI* botAI) : GenericWarriorStr
 std::vector<NextAction> TankWarriorStrategy::getDefaultActions()
 {
     return {
-        NextAction("devastate", ACTION_DEFAULT + 0.3f),
-        NextAction("revenge", ACTION_DEFAULT + 0.2f),
-        NextAction("demoralizing shout", ACTION_DEFAULT + 0.1f),
+        NextAction("demoralizing shout", ACTION_NORMAL + 3),
         NextAction("melee", ACTION_DEFAULT)
     };
 }
@@ -134,25 +187,16 @@ void TankWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
         new TriggerNode(
             "enemy out of melee",
             {
-                NextAction("heroic throw", ACTION_MOVE + 11),
-                NextAction("charge", ACTION_MOVE + 10)
+                NextAction("charge", ACTION_MOVE + 11)
             }
         )
     );
 
-    triggers.push_back(
-        new TriggerNode(
-            "thunder clap and rage",
-            {
-                NextAction("thunder clap", ACTION_MOVE + 11)
-            }
-        )
-    );
     triggers.push_back(
         new TriggerNode(
             "defensive stance",
             {
-                NextAction("defensive stance", ACTION_HIGH + 9)
+                NextAction("defensive stance", ACTION_NORMAL + 2)
             }
         )
     );
@@ -160,7 +204,7 @@ void TankWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
         new TriggerNode(
             "commanding shout",
             {
-                NextAction("commanding shout", ACTION_HIGH + 8)
+                NextAction("commanding shout", ACTION_HIGH)
             }
         )
     );
@@ -176,40 +220,39 @@ void TankWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
         new TriggerNode(
             "sunder armor",
             {
-                NextAction("devastate", ACTION_HIGH + 2)
+                NextAction("sunder armor", ACTION_DEFAULT + 0.4f)
             }
         )
     );
     triggers.push_back(
         new TriggerNode(
-            "medium rage available",
+            "shield block",
             {
-                NextAction("shield slam", ACTION_HIGH + 2),
-                NextAction("devastate", ACTION_HIGH + 1)
+                NextAction("shield block", ACTION_NORMAL + 5)
             }
         )
     );
     triggers.push_back(
         new TriggerNode(
-            "shield block",
+            "revenge",
             {
-                NextAction("shield block", ACTION_INTERRUPT + 1)
+                NextAction("revenge", ACTION_HIGH + 5)
             }
         )
     );
     triggers.push_back(
         new TriggerNode(
-            "revenge",
+            "disarm",
             {
-                NextAction("revenge", ACTION_HIGH + 2)
+                NextAction("disarm", ACTION_NORMAL + 4)
             }
         )
     );
     triggers.push_back(
         new TriggerNode(
-            "disarm",
+            "thunder clap",
             {
-                NextAction("disarm", ACTION_HIGH + 1)
+                NextAction("thunder clap", ACTION_NORMAL + 3)
             }
         )
     );
@@ -294,27 +337,11 @@ void TankWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
             }
         )
     );
-    triggers.push_back(
-        new TriggerNode(
-            "sword and board",
-            {
-                NextAction("shield slam", ACTION_INTERRUPT)
-            }
-        )
-    );
     triggers.push_back(
         new TriggerNode(
             "rend",
             {
-                NextAction("rend", ACTION_NORMAL + 1)
-            }
-        )
-    );
-    triggers.push_back(
-        new TriggerNode(
-        "rend on attacker",
-            {
-                NextAction("rend on attacker", ACTION_NORMAL + 1)
+                NextAction("rend", ACTION_HIGH + 3)
             }
         )
     );
@@ -328,17 +355,9 @@ void TankWarriorStrategy::InitTriggers(std::vector<TriggerNode*>& triggers)
     );
     triggers.push_back(
         new TriggerNode(
-            "high rage available",
-            {
-                NextAction("heroic strike", ACTION_HIGH)
-            }
-        )
-    );
-    triggers.push_back(
-        new TriggerNode(
-            "medium rage available",
+            "hamstring",
             {
-                NextAction("thunder clap", ACTION_HIGH + 1)
+                NextAction("hamstring", ACTION_HIGH + 4)
             }
         )
     );
diff --git a/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.cpp b/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.cpp
index 40609f5c..d20c484f 100644
--- a/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.cpp
+++ b/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.cpp
@@ -9,12 +9,5 @@
 
 std::string WarriorPullStrategy::GetPullActionName() const
 {
-    Unit* target = GetTarget();
-    if (!target)
-        return PullStrategy::GetPullActionName();
-
-    if (botAI->CanCastSpell("heroic throw", target))
-        return "heroic throw";
-
     return PullStrategy::GetPullActionName();
 }
diff --git a/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.h b/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.h
index a1b2217b..430338f1 100644
--- a/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.h
+++ b/src/Ai/Class/Warrior/Strategy/WarriorPullStrategy.h
@@ -11,7 +11,7 @@
 class WarriorPullStrategy : public PullStrategy
 {
 public:
-    WarriorPullStrategy(PlayerbotAI* botAI) : PullStrategy(botAI, "shoot") {}
+    WarriorPullStrategy(PlayerbotAI* botAI) : PullStrategy(botAI, "charge", "battle stance") {}
 
     std::string GetPullActionName() const override;
 };
diff --git a/src/Ai/Class/Warrior/WarriorActions.cpp b/src/Ai/Class/Warrior/WarriorActions.cpp
index 4636f784..e88d7fbf 100644
--- a/src/Ai/Class/Warrior/WarriorActions.cpp
+++ b/src/Ai/Class/Warrior/WarriorActions.cpp
@@ -17,6 +17,33 @@ constexpr uint32 SPELL_BLESSING_OF_PROTECTION = 41450;
 constexpr uint32 SPELL_SHATTERING_THROW = 64382;
 }
 
+bool IsValidDisarmTarget(PlayerbotAI* botAI, Unit* target)
+{
+    Player* player = target ? target->ToPlayer() : nullptr;
+    if (!player || !player->IsAlive() || botAI->HasAura("disarm", player))
+        return false;
+
+    return player->getClass() == CLASS_HUNTER;
+}
+
+bool CastDisarmAction::isPossible()
+{
+    if (!botAI->HasAura("defensive stance", bot))
+        return botAI->HasSpell(spell) && !bot->HasSpellCooldown(AI_VALUE2(uint32, "spell id", spell));
+
+    return CastDebuffSpellAction::isPossible();
+}
+
+bool CastDisarmAction::isUseful()
+{
+    return IsValidDisarmTarget(botAI, GetTarget()) && CastDebuffSpellAction::isUseful();
+}
+
+bool CastDisarmOnAttackerAction::isUseful()
+{
+    return IsValidDisarmTarget(botAI, GetTarget()) && CastDebuffSpellOnAttackerAction::isUseful();
+}
+
 bool CastBerserkerRageAction::isPossible()
 {
     if (botAI->IsInVehicle() && !botAI->IsInVehicle(false, false, true))
@@ -46,6 +73,27 @@ bool CastBerserkerRageAction::isUseful()
 
 bool CastSunderArmorAction::isUseful()
 {
+    Unit* target = GetTarget();
+    if (!target || !target->ToPlayer() || !botAI->IsCaster(target->ToPlayer()) ||
+        AI_VALUE2(uint8, "rage", "self target") < 60)
+    {
+        return false;
+    }
+
+    if (botAI->HasSpell("hamstring"))
+    {
+        Aura* aura = botAI->GetAura("hamstring", target, false, true);
+        if (!aura || aura->GetDuration() <= 2000)
+            return false;
+    }
+
+    if (botAI->HasSpell("rend"))
+    {
+        Aura* aura = botAI->GetAura("rend", target, false, true);
+        if (!aura || aura->GetDuration() <= 2000)
+            return false;
+    }
+
     Group* group = bot->GetGroup();
     if (!group)
         return false;
@@ -71,6 +119,14 @@ bool CastSunderArmorAction::isUseful()
     return !aura || aura->GetStackAmount() < 5 || aura->GetDuration() <= 6000;
 }
 
+bool CastHamstringAction::isPossible()
+{
+    if (!botAI->HasAura("battle stance", bot))
+        return botAI->HasSpell(spell) && !bot->HasSpellCooldown(AI_VALUE2(uint32, "spell id", spell));
+
+    return CastDebuffSpellAction::isPossible();
+}
+
 Unit* CastVigilanceAction::GetTarget()
 {
     Group* group = bot->GetGroup();
diff --git a/src/Ai/Class/Warrior/WarriorActions.h b/src/Ai/Class/Warrior/WarriorActions.h
index f1316c2c..c940a6a9 100644
--- a/src/Ai/Class/Warrior/WarriorActions.h
+++ b/src/Ai/Class/Warrior/WarriorActions.h
@@ -12,6 +12,8 @@
 #include "PlayerbotAI.h"
 #include "ReachTargetActions.h"
 
+bool IsValidDisarmTarget(PlayerbotAI* botAI, Unit* target);
+
 // stances
 BUFF_ACTION(CastBattleStanceAction, "battle stance");
 BUFF_ACTION(CastDefensiveStanceAction, "defensive stance");
@@ -56,7 +58,13 @@ public:
 
 MELEE_ACTION(CastThunderClapAction, "thunder clap");
 SNARE_ACTION(CastThunderClapSnareAction, "thunder clap");
-SNARE_ACTION(CastHamstringAction, "hamstring");
+class CastHamstringAction : public CastDebuffSpellAction
+{
+public:
+    CastHamstringAction(PlayerbotAI* botAI) : CastDebuffSpellAction(botAI, "hamstring", true) {}
+
+    bool isPossible() override;
+};
 MELEE_ACTION(CastOverpowerAction, "overpower");
 MELEE_ACTION(CastMockingBlowAction, "mocking blow");
 // BUFF_ACTION(CastRetaliationAction, "retaliation");
@@ -112,10 +120,24 @@ MELEE_ACTION(CastShieldBashAction, "shield bash");
 ENEMY_HEALER_ACTION(CastShieldBashOnEnemyHealerAction, "shield bash");
 MELEE_ACTION(CastRevengeAction, "revenge");
 BUFF_ACTION(CastShieldBlockAction, "shield block");
-DEBUFF_ACTION_U(CastDisarmAction, "disarm",
-                GetTarget() && GetTarget()->IsPlayer() ? !botAI->IsRanged((Player*)GetTarget())
-                                                       : CastDebuffSpellAction::isUseful());
-DEBUFF_ENEMY_ACTION(CastDisarmOnAttackerAction, "disarm");
+bool IsValidDisarmTarget(PlayerbotAI* botAI, Unit* target);
+
+class CastDisarmAction : public CastDebuffSpellAction
+{
+public:
+    CastDisarmAction(PlayerbotAI* botAI) : CastDebuffSpellAction(botAI, "disarm", false, 0.0f) {}
+
+    bool isPossible() override;
+    bool isUseful() override;
+};
+
+class CastDisarmOnAttackerAction : public CastDebuffSpellOnAttackerAction
+{
+public:
+    CastDisarmOnAttackerAction(PlayerbotAI* botAI) : CastDebuffSpellOnAttackerAction(botAI, "disarm", false, 0.0f) {}
+
+    bool isUseful() override;
+};
 BUFF_ACTION(CastShieldWallAction, "shield wall");
 // protection 2.4.3
 PROTECT_ACTION(CastInterveneAction, "intervene");
diff --git a/src/Ai/Class/Warrior/WarriorAiObjectContext.cpp b/src/Ai/Class/Warrior/WarriorAiObjectContext.cpp
index 78130772..ec3d01c8 100644
--- a/src/Ai/Class/Warrior/WarriorAiObjectContext.cpp
+++ b/src/Ai/Class/Warrior/WarriorAiObjectContext.cpp
@@ -93,7 +93,6 @@ public:
         creators["shockwave"] = &WarriorTriggerFactoryInternal::shockwave;
         creators["shockwave on snare target"] = &WarriorTriggerFactoryInternal::shockwave_on_snare_target;
         creators["taste for blood"] = &WarriorTriggerFactoryInternal::taste_for_blood;
-
         creators["thunder clap and rage"] = &WarriorTriggerFactoryInternal::thunderclap_and_rage;
         creators["intercept can cast"] = &WarriorTriggerFactoryInternal::intercept_can_cast;
         creators["intercept and far enemy"] = &WarriorTriggerFactoryInternal::intercept_and_far_enemy;
@@ -109,7 +108,7 @@ private:
     static Trigger* defensive_stance(PlayerbotAI* botAI) { return new DefensiveStanceTrigger(botAI); }
     static Trigger* berserker_stance(PlayerbotAI* botAI) { return new BerserkerStanceTrigger(botAI); }
     static Trigger* battle_stance(PlayerbotAI* botAI) { return new BattleStanceTrigger(botAI); }
-    static Trigger* hamstring(PlayerbotAI* botAI) { return new HamstringTrigger(botAI); }
+    static Trigger* hamstring(PlayerbotAI* botAI) { return new HamstringDebuffTrigger(botAI); }
     static Trigger* victory_rush(PlayerbotAI* botAI) { return new VictoryRushTrigger(botAI); }
     static Trigger* death_wish(PlayerbotAI* botAI) { return new DeathWishTrigger(botAI); }
     static Trigger* recklessness(PlayerbotAI* botAI) { return new RecklessnessTrigger(botAI); }
diff --git a/src/Ai/Class/Warrior/WarriorTriggers.cpp b/src/Ai/Class/Warrior/WarriorTriggers.cpp
index e875c403..7f8eea69 100644
--- a/src/Ai/Class/Warrior/WarriorTriggers.cpp
+++ b/src/Ai/Class/Warrior/WarriorTriggers.cpp
@@ -4,7 +4,10 @@
  */
 
 #include "WarriorTriggers.h"
+#include "Item.h"
+#include "ItemTemplate.h"
 #include "Playerbots.h"
+#include "WarriorActions.h"
 
 namespace
 {
@@ -16,6 +19,40 @@ constexpr uint32 SPELL_BLESSING_OF_PROTECTION = 41450;
 constexpr uint32 SPELL_COMMANDING_PRESENCE_RANKS[] = { 12318, 12857, 12858, 12860, 12861 };
 }
 
+bool BattleStanceTrigger::IsActive()
+{
+    if (BuffTrigger::IsActive())
+    {
+        Unit* target = AI_VALUE(Unit*, "current target");
+        if (target && IsValidDisarmTarget(botAI, target) && !botAI->HasAura("disarm", target))
+            return false;
+
+        return true;
+    }
+    return false;
+}
+
+bool ShieldBlockTrigger::IsActive()
+{
+    Item* shield = bot->GetItemByPos(INVENTORY_SLOT_BAG_0, EQUIPMENT_SLOT_OFFHAND);
+    if (!shield || shield->GetTemplate()->SubClass != ITEM_SUBCLASS_ARMOR_SHIELD)
+        return false;
+
+    if (!botAI->HasAura("defensive stance", bot))
+        return false;
+
+    return BuffTrigger::IsActive();
+}
+
+bool DisarmDebuffTrigger::IsActive()
+{
+    Unit* target = GetTarget();
+    if (!target || !IsValidDisarmTarget(botAI, target))
+        return false;
+
+    return DebuffTrigger::IsActive();
+}
+
 bool BloodrageBuffTrigger::IsActive()
 {
     return AI_VALUE2(uint8, "health", "self target") >= sPlayerbotAIConfig.mediumHealth &&
diff --git a/src/Ai/Class/Warrior/WarriorTriggers.h b/src/Ai/Class/Warrior/WarriorTriggers.h
index 15865744..fd1b39e2 100644
--- a/src/Ai/Class/Warrior/WarriorTriggers.h
+++ b/src/Ai/Class/Warrior/WarriorTriggers.h
@@ -16,12 +16,31 @@ public:
     bool IsActive() override;
 };
 
-BUFF_TRIGGER(BattleStanceTrigger, "battle stance");
+class BattleStanceTrigger : public BuffTrigger
+{
+public:
+    BattleStanceTrigger(PlayerbotAI* botAI) : BuffTrigger(botAI, "battle stance") {}
+    bool IsActive() override;
+};
+
 BUFF_TRIGGER(DefensiveStanceTrigger, "defensive stance");
 BUFF_TRIGGER(BerserkerStanceTrigger, "berserker stance");
-BUFF_TRIGGER(ShieldBlockTrigger, "shield block");
+
+class ShieldBlockTrigger : public BuffTrigger
+{
+public:
+    ShieldBlockTrigger(PlayerbotAI* botAI) : BuffTrigger(botAI, "shield block") {}
+    bool IsActive() override;
+};
+
 BUFF_TRIGGER(CommandingShoutTrigger, "commanding shout");
-DEBUFF_TRIGGER(DisarmDebuffTrigger, "disarm");
+
+class DisarmDebuffTrigger : public DebuffTrigger
+{
+public:
+    DisarmDebuffTrigger(PlayerbotAI* botAI) : DebuffTrigger(botAI, "disarm", 1, false, 0.0f) {}
+    bool IsActive() override;
+};
 DEBUFF_TRIGGER(SunderArmorDebuffTrigger, "sunder armor");
 DEBUFF_TRIGGER(MortalStrikeDebuffTrigger, "mortal strike");
 // DEBUFF_ENEMY_TRIGGER(RendDebuffOnAttackerTrigger, "rend");
@@ -39,7 +58,12 @@ BUFF_TRIGGER_A(BloodrageBuffTrigger, "bloodrage");
 CAN_CAST_TRIGGER(VictoryRushTrigger, "victory rush");
 HAS_AURA_TRIGGER(SwordAndBoardTrigger, "sword and board");
 SNARE_TRIGGER(ConcussionBlowTrigger, "concussion blow");
-SNARE_TRIGGER(HamstringTrigger, "hamstring");
+class HamstringDebuffTrigger : public DebuffTrigger
+{
+public:
+    HamstringDebuffTrigger(PlayerbotAI* botAI)
+        : DebuffTrigger(botAI, "hamstring", 1, true, 8.0f, 2000) {}
+};
 SNARE_TRIGGER(MockingBlowTrigger, "mocking blow");
 SNARE_TRIGGER(ThunderClapSnareTrigger, "thunder clap");
 DEBUFF_TRIGGER(ThunderClapTrigger, "thunder clap");
@@ -67,7 +91,7 @@ HAS_AURA_TRIGGER(TasteForBloodTrigger, "taste for blood");
 class RendDebuffTrigger : public DebuffTrigger
 {
 public:
-    RendDebuffTrigger(PlayerbotAI* botAI) : DebuffTrigger(botAI, "rend", 1, true) {}
+    RendDebuffTrigger(PlayerbotAI* botAI) : DebuffTrigger(botAI, "rend", 1, true, 8.0f, 2000) {}
 };
 
 class VigilanceTrigger : public BuffOnPartyTrigger
diff --git a/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp b/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp
index f1eb0d7e..e1ba88f8 100644
--- a/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp
+++ b/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp
@@ -307,19 +307,6 @@ bool NewRpgBaseAction::InteractWithNpcOrGameObjectForQuest(ObjectGuid guid)
             continue;
 
         const QuestStatus& status = bot->GetQuestStatus(item.QuestId);
-        if (status == QUEST_STATUS_NONE && bot->CanTakeQuest(quest, false) && bot->CanAddQuest(quest, false) &&
-            IsQuestWorthDoing(quest) && IsQuestCapableDoing(quest))
-        {
-            AcceptQuest(quest, guid);
-            if (botAI->GetMaster())
-                botAI->TellMasterNoFacing(PlayerbotTextMgr::instance().GetBotTextOrDefault(
-                    "new_rpg_quest_accepted",
-                    "Quest accepted %quest",
-                    {{"%quest", ChatHelper::FormatQuest(quest)}}));
-            BroadcastHelper::BroadcastQuestAccepted(botAI, bot, quest);
-            botAI->rpgStatistic.questAccepted++;
-            LOG_DEBUG("playerbots", "[New RPG] {} accept quest {}", bot->GetName(), quest->GetQuestId());
-        }
         if (status == QUEST_STATUS_COMPLETE && bot->CanRewardQuest(quest, 0, false))
         {
             TurnInQuest(quest, guid);
@@ -459,17 +446,6 @@ bool NewRpgBaseAction::IsWithinInteractionDist(Object* questGiver)
     return false;
 }
 
-bool NewRpgBaseAction::AcceptQuest(Quest const* quest, ObjectGuid guid)
-{
-    WorldPacket p(CMSG_QUESTGIVER_ACCEPT_QUEST);
-    uint32 unk1 = 0;
-    p << guid << quest->GetQuestId() << unk1;
-    p.rpos(0);
-    bot->GetSession()->HandleQuestgiverAcceptQuestOpcode(p);
-
-    return true;
-}
-
 bool NewRpgBaseAction::TurnInQuest(Quest const* quest, ObjectGuid guid)
 {
     uint32 questID = quest->GetQuestId();
@@ -783,20 +759,6 @@ bool NewRpgBaseAction::HasQuestToAcceptOrReward(WorldObject* object)
             return true;
         }
     }
-    for (uint8 idx = 0; idx < menu.GetMenuItemCount(); idx++)
-    {
-        const QuestMenuItem& item = menu.GetItem(idx);
-        const Quest* quest = sObjectMgr->GetQuestTemplate(item.QuestId);
-        if (!quest)
-            continue;
-
-        const QuestStatus& status = bot->GetQuestStatus(item.QuestId);
-        if (status == QUEST_STATUS_NONE && bot->CanTakeQuest(quest, false) && bot->CanAddQuest(quest, false) &&
-            IsQuestWorthDoing(quest) && IsQuestCapableDoing(quest))
-        {
-            return true;
-        }
-    }
     return false;
 }
 
diff --git a/src/Ai/World/Rpg/Action/NewRpgBaseAction.h b/src/Ai/World/Rpg/Action/NewRpgBaseAction.h
index 1cef54ab..7f103634 100644
--- a/src/Ai/World/Rpg/Action/NewRpgBaseAction.h
+++ b/src/Ai/World/Rpg/Action/NewRpgBaseAction.h
@@ -46,7 +46,6 @@ protected:
 
     /* QUEST RELATED ACTION */
     bool SearchQuestGiverAndAcceptOrReward();
-    bool AcceptQuest(Quest const* quest, ObjectGuid guid);
     bool TurnInQuest(Quest const* quest, ObjectGuid guid);
     bool OrganizeQuestLog();
 
diff --git a/src/Bot/Factory/PlayerbotFactory.cpp b/src/Bot/Factory/PlayerbotFactory.cpp
index 2ef0f539..32f58752 100644
--- a/src/Bot/Factory/PlayerbotFactory.cpp
+++ b/src/Bot/Factory/PlayerbotFactory.cpp
@@ -1078,32 +1078,6 @@ void PlayerbotFactory::InitConsumables()
             }
             break;
         }
-        case CLASS_ROGUE:
-        {
-            std::vector<uint32> instant_poisons = {
-                INSTANT_POISON_IX, INSTANT_POISON_VIII, INSTANT_POISON_VII, INSTANT_POISON_VI, INSTANT_POISON_V,
-                INSTANT_POISON_IV, INSTANT_POISON_III, INSTANT_POISON_II, INSTANT_POISON };
-            std::vector<uint32> deadly_poisons = {
-                DEADLY_POISON_IX, DEADLY_POISON_VIII, DEADLY_POISON_VII, DEADLY_POISON_VI, DEADLY_POISON_V,
-                DEADLY_POISON_IV, DEADLY_POISON_III, DEADLY_POISON_II, DEADLY_POISON };
-            for (uint32 itemId : deadly_poisons)
-            {
-                ItemTemplate const* proto = sObjectMgr->GetItemTemplate(itemId);
-                if (proto->RequiredLevel > level)
-                    continue;
-                items.push_back({itemId, 20});
-                break;
-            }
-            for (uint32 itemId : instant_poisons)
-            {
-                ItemTemplate const* proto = sObjectMgr->GetItemTemplate(itemId);
-                if (proto->RequiredLevel > level)
-                    continue;
-                items.push_back({itemId, 20});
-                break;
-            }
-            break;
-        }
         default:
             break;
     }
@@ -3232,6 +3206,9 @@ void PlayerbotFactory::InitAvailableSpells()
             if (!trainerSpell)
                 continue;
 
+            if (sPlayerbotAIConfig.disabledSpellIds.contains(trainerSpell->SpellId))
+                continue;
+
             if (!trainer->CanTeachSpell(bot, trainerSpell))
                 continue;
 
@@ -3353,6 +3330,9 @@ void PlayerbotFactory::InitSpecialSpells()
     {
         bot->learnSpell(50977, false);
     }
+
+    for (uint32 spellId : sPlayerbotAIConfig.disabledSpellIds)
+        bot->removeSpell(spellId, SPEC_MASK_ALL, false);
 }
 
 void PlayerbotFactory::InitTalents(uint32 specNo)
diff --git a/src/Bot/Factory/RandomPlayerbotFactory.cpp b/src/Bot/Factory/RandomPlayerbotFactory.cpp
index 002720ac..1debec51 100644
--- a/src/Bot/Factory/RandomPlayerbotFactory.cpp
+++ b/src/Bot/Factory/RandomPlayerbotFactory.cpp
@@ -586,6 +586,14 @@ void RandomPlayerbotFactory::CreateRandomBots()
         return;
     }
 
+    if (sPlayerbotAIConfig.maxRandomBots == 0 &&
+        sPlayerbotAIConfig.addClassAccountPoolSize == 0 &&
+        sPlayerbotAIConfig.randomBotAccountCount == 0)
+    {
+        LOG_INFO("playerbots", "Random bot and AddClass account creation disabled.");
+        return;
+    }
+
     LOG_INFO("playerbots", "Creating random bot accounts...");
     std::unordered_map<NameRaceAndGender, std::vector<std::string>> nameCache;
     std::vector<std::future<void>> account_creations;
diff --git a/src/Bot/PlayerbotAI.cpp b/src/Bot/PlayerbotAI.cpp
index cd7a0993..3bd96b5a 100644
--- a/src/Bot/PlayerbotAI.cpp
+++ b/src/Bot/PlayerbotAI.cpp
@@ -5,12 +5,15 @@
 
 #include "PlayerbotAI.h"
 
+#include <algorithm>
 #include <cmath>
+#include <cctype>
 #include <mutex>
 #include <sstream>
 #include <string>
 
 #include "AiFactory.h"
+#include "BattlegroundWS.h"
 #include "BudgetValues.h"
 #include "ChannelMgr.h"
 #include "CharacterPackets.h"
@@ -34,8 +37,10 @@
 #include "MotionMaster.h"
 #include "MoveSplineInit.h"
 #include "NewRpgStrategy.h"
+#include "ObjectAccessor.h"
 #include "ObjectGuid.h"
 #include "ObjectMgr.h"
+#include "Opcodes.h"
 #include "PerfMonitor.h"
 #include "Player.h"
 #include "PlayerbotTextMgr.h"
@@ -60,6 +65,14 @@
 
 constexpr uint32 SPELL_TITAN_GRIP = 49152;
 constexpr uint32 SPELL_DK_FROST_PRESENCE = 48263;
+constexpr uint32 DISPEL_COOLDOWN_MS = 8000;
+
+bool IsDispelSpell(SpellInfo const* spellInfo)
+{
+    return spellInfo && (spellInfo->HasEffect(SPELL_EFFECT_DISPEL) ||
+                         spellInfo->HasEffect(SPELL_EFFECT_DISPEL_MECHANIC) ||
+                         spellInfo->HasEffect(SPELL_EFFECT_STEAL_BENEFICIAL_BUFF));
+}
 
 std::vector<std::string> PlayerbotAI::dispel_whitelist = {
     "mutating injection",
@@ -548,7 +561,12 @@ void PlayerbotAI::UpdateAIInternal([[maybe_unused]] uint32 elapsed, bool minimal
     masterIncomingPacketHandlers.Handle(helper);
     masterOutgoingPacketHandlers.Handle(helper);
 
-    DoNextAction(minimal);
+    if ((!bot->InBattleground() || !bot->IsAlive()) && !battlegroundCommand.empty())
+        ResetBattlegroundCommand();
+
+    HandleFlagDropRequest();
+    if (!flagDropTarget)
+        DoNextAction(minimal);
 
     if (pmo)
         pmo->finish();
@@ -592,6 +610,235 @@ void PlayerbotAI::HandleCommands()
     }
 }
 
+Player* PlayerbotAI::GetBattlegroundCommandTarget()
+{
+    return battlegroundCommandTarget ? GetPlayer(battlegroundCommandTarget) : nullptr;
+}
+
+void PlayerbotAI::SayBattlegroundCommand(std::string const& text)
+{
+    Battleground* bg = bot->GetBattleground();
+    if (!bg)
+        return;
+
+    WorldPacket data;
+    ChatHandler::BuildChatPacket(data, CHAT_MSG_BATTLEGROUND, text.c_str(), LANG_UNIVERSAL, CHAT_TAG_NONE,
+                                 bot->GetGUID(), bot->GetName());
+    bg->SendPacketToTeam(bot->GetBgTeamId(), &data, bot, true);
+}
+
+void PlayerbotAI::ResetBattlegroundCommand()
+{
+    if (battlegroundCommand == "stay")
+    {
+        ChangeStrategy("-stay", BOT_STATE_NON_COMBAT);
+        ChangeStrategy("-stay", BOT_STATE_COMBAT);
+        aiObjectContext->GetValue<PositionInfo>("pos", "stay")->Reset();
+    }
+    else if (battlegroundCommand == "come")
+    {
+        ChangeStrategy("-follow", BOT_STATE_NON_COMBAT);
+        ChangeStrategy("-follow", BOT_STATE_COMBAT);
+    }
+    else if (battlegroundCommand == "flee")
+    {
+        ChangeStrategy("-runaway", BOT_STATE_NON_COMBAT);
+        ChangeStrategy("-runaway", BOT_STATE_COMBAT);
+    }
+    else if (battlegroundCommand == "focus")
+    {
+        aiObjectContext->GetValue<GuidVector>("prioritized targets")->Reset();
+        aiObjectContext->GetValue<Unit*>("current target")->Set(nullptr);
+        bot->SetTarget();
+    }
+    else if (battlegroundCommand == "support")
+    {
+        std::list<ObjectGuid> const noFocusTargets;
+        aiObjectContext->GetValue<std::list<ObjectGuid>>("focus heal targets")->Set(noFocusTargets);
+        ChangeStrategy("-focus heal targets", BOT_STATE_COMBAT);
+    }
+
+    battlegroundCommand.clear();
+    battlegroundCommandTarget = ObjectGuid::Empty;
+    flagDropTarget = ObjectGuid::Empty;
+}
+
+bool PlayerbotAI::HandleGlobalBattlegroundCommand(uint32 type, std::string const& text, Player* fromPlayer)
+{
+    if (!bot->InBattleground() || (type != CHAT_MSG_BATTLEGROUND && type != CHAT_MSG_BATTLEGROUND_LEADER))
+        return false;
+
+    if (!fromPlayer || fromPlayer->GetBattleground() != bot->GetBattleground() ||
+        fromPlayer->GetBgTeamId() != bot->GetBgTeamId())
+        return true;
+
+    std::string command = text;
+    trim(command);
+    std::string lowerCommand = command;
+    std::transform(lowerCommand.begin(), lowerCommand.end(), lowerCommand.begin(),
+                   [](unsigned char c) { return std::tolower(c); });
+
+    size_t separator = lowerCommand.find_first_of(" \t");
+    if (separator == std::string::npos)
+        return false;
+
+    std::string keyword = lowerCommand.substr(0, separator);
+    std::string targetName = command.substr(separator + 1);
+    trim(targetName);
+    if (targetName.empty() || !normalizePlayerName(targetName))
+        return false;
+
+    bool focus = keyword == "focus" || keyword == "attack" || keyword == "kill";
+    bool support = keyword == "support" || keyword == "help" || keyword == "heal";
+    if (!focus && !support)
+        return false;
+
+    Player* target = ObjectAccessor::FindPlayerByName(targetName.c_str());
+    if (!IsValidPlayer(target) || target->GetBattleground() != bot->GetBattleground() || !target->IsAlive())
+        return true;
+
+    if ((focus && target->GetBgTeamId() == bot->GetBgTeamId()) ||
+        (support && target->GetBgTeamId() != bot->GetBgTeamId()))
+        return true;
+
+    if (ServerFacade::instance().IsDistanceGreaterThan(ServerFacade::instance().GetDistance2d(bot, target), 100.0f))
+        return true;
+
+    ResetBattlegroundCommand();
+    battlegroundCommand = focus ? "focus" : "support";
+
+    if (focus)
+    {
+        aiObjectContext->GetValue<GuidVector>("prioritized targets")->Set({target->GetGUID()});
+        aiObjectContext->GetValue<Unit*>("current target")->Set(target);
+        bot->SetTarget(target->GetGUID());
+        DoSpecificAction("attack", Event("bg command", "", fromPlayer), true);
+    }
+    else
+    {
+        std::list<ObjectGuid> const focusTargets = {target->GetGUID()};
+        aiObjectContext->GetValue<std::list<ObjectGuid>>("focus heal targets")->Set(focusTargets);
+        ChangeStrategy("+focus heal targets", BOT_STATE_COMBAT);
+    }
+
+    return true;
+}
+
+bool PlayerbotAI::HandleBattlegroundCommand(uint32 type, std::string const& text, Player* fromPlayer)
+{
+    if (!bot->InBattleground() || (type != CHAT_MSG_WHISPER && type != CHAT_MSG_BATTLEGROUND &&
+                                   type != CHAT_MSG_BATTLEGROUND_LEADER))
+        return false;
+
+    if (!fromPlayer || fromPlayer->GetBattleground() != bot->GetBattleground() ||
+        fromPlayer->GetBgTeamId() != bot->GetBgTeamId())
+        return true;
+
+    if (type != CHAT_MSG_WHISPER && !strstri(text.c_str(), bot->GetName().c_str()))
+        return true;
+
+    bool pass = strstri(text.c_str(), "pass") || strstri(text.c_str(), "give") || strstri(text.c_str(), "drop");
+    bool stay = strstri(text.c_str(), "stay") || strstri(text.c_str(), "wait") ||
+                strstri(text.c_str(), "hold") || strstri(text.c_str(), "defend") || strstri(text.c_str(), "guard");
+    bool come = strstri(text.c_str(), "follow") || strstri(text.c_str(), "come") ||
+                strstri(text.c_str(), "assist") || strstri(text.c_str(), "regroup");
+    bool flee = strstri(text.c_str(), "flee") || strstri(text.c_str(), "runaway") ||
+                strstri(text.c_str(), "run away") || strstri(text.c_str(), "retreat");
+
+    std::string action;
+    if (pass && bot->GetBattlegroundTypeId() == BATTLEGROUND_WS &&
+        (bot->HasAura(BG_WS_SPELL_WARSONG_FLAG) || bot->HasAura(BG_WS_SPELL_SILVERWING_FLAG)))
+    {
+        ResetBattlegroundCommand();
+        battlegroundCommand = "pass";
+        flagDropTarget = fromPlayer->GetGUID();
+        action = "pass the flag";
+    }
+    else if (stay)
+    {
+        ResetBattlegroundCommand();
+        battlegroundCommand = "stay";
+        if (!DoSpecificAction("stay chat shortcut", Event("bg command", "", fromPlayer), true))
+        {
+            ResetBattlegroundCommand();
+            return true;
+        }
+        action = "stay";
+    }
+    else if (come)
+    {
+        ResetBattlegroundCommand();
+        battlegroundCommand = "come";
+        battlegroundCommandTarget = fromPlayer->GetGUID();
+        if (!DoSpecificAction("follow chat shortcut", Event("bg command", "", fromPlayer), true))
+        {
+            ResetBattlegroundCommand();
+            return true;
+        }
+        action = "come";
+    }
+    else if (flee)
+    {
+        ResetBattlegroundCommand();
+        battlegroundCommand = "flee";
+        if (!DoSpecificAction("runaway chat shortcut", Event("bg command", "", fromPlayer), true))
+        {
+            ResetBattlegroundCommand();
+            return true;
+        }
+        action = "flee";
+    }
+    else
+    {
+        if (type == CHAT_MSG_WHISPER)
+            bot->Whisper("You can ask me to stay, come, flee or pass the flag", LANG_UNIVERSAL, fromPlayer);
+        return true;
+    }
+
+    SayBattlegroundCommand("Ok " + fromPlayer->GetName() + ", I will " + action + " now.");
+    return true;
+}
+
+void PlayerbotAI::HandleFlagDropRequest()
+{
+    if (!flagDropTarget)
+        return;
+
+    if (!bot->InBattleground() || bot->GetBattlegroundTypeId() != BATTLEGROUND_WS)
+    {
+        ResetBattlegroundCommand();
+        return;
+    }
+
+    if (!bot->HasAura(BG_WS_SPELL_WARSONG_FLAG) && !bot->HasAura(BG_WS_SPELL_SILVERWING_FLAG))
+    {
+        ResetBattlegroundCommand();
+        return;
+    }
+
+    Player* target = GetPlayer(flagDropTarget);
+    if (!target || !target->IsAlive() || target->GetTeamId() != bot->GetTeamId() ||
+        target->GetBattleground() != bot->GetBattleground() || !IsSafe(target))
+    {
+        ResetBattlegroundCommand();
+        return;
+    }
+
+    if (bot->GetDistance(target) <= INTERACTION_DISTANCE)
+    {
+        uint32 flagSpell = bot->HasAura(BG_WS_SPELL_WARSONG_FLAG) ? BG_WS_SPELL_WARSONG_FLAG
+                                                                    : BG_WS_SPELL_SILVERWING_FLAG;
+        WorldPacket packet(CMSG_CANCEL_AURA, sizeof(uint32));
+        packet << flagSpell;
+        bot->GetSession()->HandleCancelAuraOpcode(packet);
+        ResetBattlegroundCommand();
+        bot->StopMoving();
+        return;
+    }
+
+    bot->GetMotionMaster()->MoveChase(target, 2.0f);
+}
+
 std::map<std::string, ChatMsg> chatMap;
 void PlayerbotAI::HandleCommand(uint32 type, const std::string& text, Player& fromPlayer, const uint32 lang)
 {
@@ -946,6 +1193,15 @@ bool PlayerbotAI::IsAllowedCommand(std::string const text)
 
 void PlayerbotAI::HandleCommand(uint32 type, std::string const text, Player* fromPlayer)
 {
+    if (!bot)
+        return;
+
+    if (HandleGlobalBattlegroundCommand(type, text, fromPlayer))
+        return;
+
+    if (HandleBattlegroundCommand(type, text, fromPlayer))
+        return;
+
     if (!GetSecurity()->CheckLevelFor(PLAYERBOT_SECURITY_INVITE, type != CHAT_MSG_WHISPER, fromPlayer))
         return;
 
@@ -2944,6 +3200,9 @@ bool PlayerbotAI::Say(const std::string& msg)
 
 bool PlayerbotAI::Whisper(const std::string& msg, const std::string& receiverName)
 {
+    if (sRandomPlayerbotMgr.IsRandomBot(bot) && !sPlayerbotAIConfig.randomBotTalk)
+        return false;
+
     const auto receiver = ObjectAccessor::FindPlayerByName(receiverName);
     if (!receiver)
     {
@@ -2969,6 +3228,9 @@ bool PlayerbotAI::TellMasterNoFacing(std::ostringstream& stream, PlayerbotSecuri
 
 bool PlayerbotAI::TellMasterNoFacing(std::string const text, PlayerbotSecurityLevel securityLevel)
 {
+    if (sRandomPlayerbotMgr.IsRandomBot(bot) && !sPlayerbotAIConfig.randomBotTalk)
+        return false;
+
     Player* master = GetMaster();
     PlayerbotAI* masterBotAI = nullptr;
     if (master)
@@ -3255,6 +3517,9 @@ bool PlayerbotAI::CanCastSpell(std::string const name, Unit* target, Item* itemT
 
 bool PlayerbotAI::CanCastSpell(uint32 spellid, Unit* target, bool checkHasSpell, Item* itemTarget, Item* castItem)
 {
+    if (sPlayerbotAIConfig.disabledSpellIds.contains(spellid))
+        return false;
+
     if (!spellid)
     {
         if (!sPlayerbotAIConfig.logInGroupOnly || (bot->GetGroup() && HasRealPlayerMaster()))
@@ -3435,6 +3700,9 @@ bool PlayerbotAI::CanCastSpell(uint32 spellid, Unit* target, bool checkHasSpell,
 
 bool PlayerbotAI::CanCastSpell(uint32 spellid, GameObject* goTarget, bool checkHasSpell)
 {
+    if (sPlayerbotAIConfig.disabledSpellIds.contains(spellid))
+        return false;
+
     if (!spellid)
         return false;
 
@@ -3493,6 +3761,9 @@ bool PlayerbotAI::CanCastSpell(uint32 spellid, GameObject* goTarget, bool checkH
 
 bool PlayerbotAI::CanCastSpell(uint32 spellid, float x, float y, float z, bool checkHasSpell, Item* itemTarget)
 {
+    if (sPlayerbotAIConfig.disabledSpellIds.contains(spellid))
+        return false;
+
     if (!spellid)
         return false;
 
@@ -3556,6 +3827,9 @@ bool PlayerbotAI::CastSpell(std::string const name, Unit* target, Item* itemTarg
 
 bool PlayerbotAI::CastSpell(uint32 spellId, Unit* target, Item* itemTarget)
 {
+    if (sPlayerbotAIConfig.disabledSpellIds.contains(spellId))
+        return false;
+
     if (!spellId)
         return false;
 
@@ -3569,7 +3843,15 @@ bool PlayerbotAI::CastSpell(uint32 spellId, Unit* target, Item* itemTarget)
     if (!spellInfo)
         return false;
 
+    uint32 now = getMSTime();
+    bool isDispel = IsDispelSpell(spellInfo);
+    if (isDispel && lastDispelCast && getMSTimeDiff(lastDispelCast, now) < DISPEL_COOLDOWN_MS)
+        return false;
+
     Pet* pet = bot->GetPet();
+    if (!bot->HasSpell(spellId) && (!pet || !pet->HasSpell(spellId)))
+        return false;
+
     if (pet && pet->HasSpell(spellId))
     {
         // List of spell IDs for which we do NOT want to toggle auto-cast or send message
@@ -3835,16 +4117,25 @@ bool PlayerbotAI::CastSpell(uint32 spellId, Unit* target, Item* itemTarget)
         TellMasterNoFacing(out);
     }
 
+    if (isDispel)
+        lastDispelCast = now;
+
     return true;
 }
 
 bool PlayerbotAI::CastSpell(uint32 spellId, float x, float y, float z, Item* itemTarget)
 {
+    if (sPlayerbotAIConfig.disabledSpellIds.contains(spellId))
+        return false;
+
     if (!spellId)
         return false;
 
     Pet* pet = bot->GetPet();
     SpellInfo const* spellInfo = sSpellMgr->GetSpellInfo(spellId);
+    if (!bot->HasSpell(spellId) && (!pet || !pet->HasSpell(spellId)))
+        return false;
+
     if (pet && pet->HasSpell(spellId))
     {
         bool autocast = false;
@@ -4837,6 +5128,9 @@ void PlayerbotAI::RemoveShapeshift()
     RemoveAura("aquatic form");
     RemoveAura("ghost wolf");
     // RemoveAura("tree of life");
+
+    // Flight forms override MOVE_RUN manually in CheckMountStateAction; restore the aura-derived rate when leaving them.
+    bot->UpdateSpeed(MOVE_RUN, true);
 }
 
 // Mirrors Blizzard’s GetAverageItemLevel rules :
diff --git a/src/Bot/PlayerbotAI.h b/src/Bot/PlayerbotAI.h
index 7940ee31..327570fa 100644
--- a/src/Bot/PlayerbotAI.h
+++ b/src/Bot/PlayerbotAI.h
@@ -391,6 +391,7 @@ public:
 
     std::string const HandleRemoteCommand(std::string const command);
     void HandleCommand(uint32 type, std::string const text, Player* fromPlayer);
+    Player* GetBattlegroundCommandTarget();
     void QueueChatResponse(const ChatQueuedReply reply);
     void HandleBotOutgoingPacket(WorldPacket const& packet);
     void HandleMasterIncomingPacket(WorldPacket const& packet);
@@ -591,7 +592,6 @@ public:
     bool HasItemInInventory(uint32 itemId);
     std::vector<std::pair<const Quest*, uint32>> GetCurrentQuestsRequiringItemId(uint32 itemId);
     uint32 GetReactDelay();
-
     std::vector<const Quest*> GetAllCurrentQuests();
     std::vector<const Quest*> GetCurrentIncompleteQuests();
     std::set<uint32> GetAllCurrentQuestIds();
@@ -615,6 +615,11 @@ private:
     void UpdateAIGroupMaster();
     Item* FindItemInInventory(std::function<bool(ItemTemplate const*)> checkItem) const;
     void HandleCommands();
+    bool HandleGlobalBattlegroundCommand(uint32 type, std::string const& text, Player* fromPlayer);
+    bool HandleBattlegroundCommand(uint32 type, std::string const& text, Player* fromPlayer);
+    void ResetBattlegroundCommand();
+    void SayBattlegroundCommand(std::string const& text);
+    void HandleFlagDropRequest();
     void HandleCommand(uint32 type, const std::string& text, Player& fromPlayer, const uint32 lang = LANG_UNIVERSAL);
     inline bool IsValidUnit(const Unit* unit) const
     {
@@ -650,6 +655,10 @@ protected:
     BotCheatMask cheatMask = BotCheatMask::none;
     Position jumpDestination = Position();
     uint32 nextTransportCheck = 0;
+    uint32 lastDispelCast = 0;
+    std::string battlegroundCommand;
+    ObjectGuid battlegroundCommandTarget = ObjectGuid::Empty;
+    ObjectGuid flagDropTarget = ObjectGuid::Empty;
     bool spellInterruptRequested = false;
 };
 
diff --git a/src/Bot/RandomPlayerbotMgr.cpp b/src/Bot/RandomPlayerbotMgr.cpp
index a1064b4e..ed5d803c 100644
--- a/src/Bot/RandomPlayerbotMgr.cpp
+++ b/src/Bot/RandomPlayerbotMgr.cpp
@@ -630,6 +630,29 @@ bool RandomPlayerbotMgr::IsAccountType(uint32 accountId, uint8 accountType)
     return result != nullptr;
 }
 
+void RandomPlayerbotMgr::RegisterManualRandomBot(ObjectGuid::LowType bot, uint32 accountId, uint32 validIn)
+{
+    if (!bot || !accountId)
+        return;
+
+    if (!sPlayerbotAIConfig.IsInRandomAccountList(accountId))
+        sPlayerbotAIConfig.randomBotAccounts.push_back(accountId);
+
+    if (std::find(rndBotTypeAccounts.begin(), rndBotTypeAccounts.end(), accountId) == rndBotTypeAccounts.end())
+        rndBotTypeAccounts.push_back(accountId);
+
+    PlayerbotsDatabase.Execute(
+        "INSERT INTO playerbots_account_type (account_id, account_type) VALUES ({}, 1) "
+        "ON DUPLICATE KEY UPDATE account_type = 1, assignment_date = NOW()",
+        accountId);
+
+    SetEventValue(bot, "add", 1, validIn ? validIn : sPlayerbotAIConfig.maxRandomBotInWorldTime);
+    SetEventValue(bot, "logout", 0, 0);
+
+    if (std::find(currentBots.begin(), currentBots.end(), bot) == currentBots.end())
+        currentBots.push_back(bot);
+}
+
 // Logs-in bots in 4 phases. Phase 1 logs Alliance bots up to how much is expected according to the faction ratio,
 // and Phase 2 logs-in the remainder Horde bots to reach the total maxAllowedBotCount. If maxAllowedBotCount is not
 // reached after Phase 2, the function goes back to log-in Alliance bots and reach maxAllowedBotCount. This is done
@@ -1334,6 +1357,9 @@ void RandomPlayerbotMgr::ScheduleChangeStrategy(uint32 bot, uint32 time)
 
 bool RandomPlayerbotMgr::ProcessBot(uint32 bot)
 {
+    if (IsFixedRosterBot(bot))
+        return false;
+
     ObjectGuid botGUID = ObjectGuid::Create<HighGuid::Player>(bot);
     Player* player = GetPlayerBot(botGUID);
     PlayerbotAI* botAI = player ? GET_PLAYERBOT_AI(player) : nullptr;
@@ -1444,6 +1470,8 @@ bool RandomPlayerbotMgr::ProcessBot(uint32 bot)
 
 bool RandomPlayerbotMgr::ProcessBot(Player* bot)
 {
+    if (IsFixedRosterBot(bot->GetGUID().GetCounter()))
+        return false;
 
     PlayerbotAI* botAI = GET_PLAYERBOT_AI(bot);
     if (!botAI)
@@ -2113,12 +2141,37 @@ bool RandomPlayerbotMgr::IsRandomBot(Player* bot)
     return false;
 }
 
+bool RandomPlayerbotMgr::IsFixedRosterBot(ObjectGuid::LowType bot)
+{
+    if (!_fixedRosterBotsLoaded)
+    {
+        _fixedRosterBotsLoaded = true;
+        _fixedRosterBots.clear();
+        QueryResult result = PlayerbotsDatabase.Query("SELECT guid FROM playerbots_fixed_roster_guid");
+        if (result)
+        {
+            do
+            {
+                Field* fields = result->Fetch();
+                _fixedRosterBots.insert(fields[0].Get<uint32>());
+            } while (result->NextRow());
+        }
+    }
+    return _fixedRosterBots.find(bot) != _fixedRosterBots.end();
+}
+
 bool RandomPlayerbotMgr::IsRandomBot(ObjectGuid::LowType bot)
 {
+    if (IsFixedRosterBot(bot))
+        return true;
+
     ObjectGuid guid = ObjectGuid::Create<HighGuid::Player>(bot);
     if (!sPlayerbotAIConfig.IsInRandomAccountList(sCharacterCache->GetCharacterAccountIdByGuid(guid)))
         return false;
 
+    if (sPlayerbotAIConfig.minRandomBots == 0 && sPlayerbotAIConfig.maxRandomBots == 0)
+        return true;
+
     if (std::find(currentBots.begin(), currentBots.end(), bot) != currentBots.end())
         return true;
 
diff --git a/src/Bot/RandomPlayerbotMgr.h b/src/Bot/RandomPlayerbotMgr.h
index 91fcebb1..45b22e30 100644
--- a/src/Bot/RandomPlayerbotMgr.h
+++ b/src/Bot/RandomPlayerbotMgr.h
@@ -103,6 +103,8 @@ public:
     static bool HandlePlayerbotConsoleCommand(ChatHandler* handler, char const* args);
     bool IsRandomBot(Player* bot);
     bool IsRandomBot(ObjectGuid::LowType bot);
+    bool IsFixedRosterBot(ObjectGuid::LowType bot);
+    void ClearFixedRosterCache() { _fixedRosterBotsLoaded = false; _fixedRosterBots.clear(); }
     bool IsAddclassBot(Player* bot);
     bool IsAddclassBot(ObjectGuid::LowType bot);
     void Randomize(Player* bot);
@@ -171,6 +173,7 @@ public:
     // Account type management
     void AssignAccountTypes();
     bool IsAccountType(uint32 accountId, uint8 accountType);
+    void RegisterManualRandomBot(ObjectGuid::LowType bot, uint32 accountId, uint32 validIn = 0);
 
 protected:
     void OnBotLoginInternal(Player* const bot) override;
@@ -249,6 +252,8 @@ private:
     std::list<uint32> currentBots;
     uint32 bgBotsCount;
     uint32 playersLevel;
+    bool _fixedRosterBotsLoaded = false;
+    std::set<uint32> _fixedRosterBots;
 
     // Account lists
     std::vector<uint32> rndBotTypeAccounts;             // Accounts marked as RNDbot (type 1)
diff --git a/src/PlayerbotAIConfig.cpp b/src/PlayerbotAIConfig.cpp
index 59293f66..3a0f4660 100644
--- a/src/PlayerbotAIConfig.cpp
+++ b/src/PlayerbotAIConfig.cpp
@@ -189,6 +189,7 @@ bool PlayerbotAIConfig::Initialize()
         randomBotQuestItems);
     LoadList<std::vector<uint32>>(sConfigMgr->GetOption<std::string>("AiPlayerbot.RandomBotSpellIds", "54197"),
                                   randomBotSpellIds);
+    LoadSet<std::set<uint32>>(sConfigMgr->GetOption<std::string>("AiPlayerbot.DisabledSpellIds", ""), disabledSpellIds);
     LoadList<std::vector<uint32>>(
         sConfigMgr->GetOption<std::string>("AiPlayerbot.PvpProhibitedZoneIds",
                                            "2255,656,2361,2362,2363,976,35,2268,3425,392,541,1446,3828,3712,3738,3565,"
diff --git a/src/PlayerbotAIConfig.h b/src/PlayerbotAIConfig.h
index 91606f80..a9dcb5cb 100644
--- a/src/PlayerbotAIConfig.h
+++ b/src/PlayerbotAIConfig.h
@@ -133,6 +133,7 @@ public:
     std::vector<uint32> randomBotQuestItems;
     std::vector<uint32> randomBotAccounts;
     std::vector<uint32> randomBotSpellIds;
+    std::set<uint32> disabledSpellIds;
     std::vector<uint32> randomBotQuestIds;
     uint32 randomBotTeleportDistance;
     float randomGearLoweringChance;
diff --git a/src/Script/Playerbots.cpp b/src/Script/Playerbots.cpp
index e8630cd5..a2a78764 100644
--- a/src/Script/Playerbots.cpp
+++ b/src/Script/Playerbots.cpp
@@ -106,16 +106,7 @@ public:
             if (sPlayerbotAIConfig.enabled)
             {
                 ChatHandler(player->GetSession()).SendSysMessage(
-                    "|cff00ff00This server runs with |cff00ccffmod-playerbots|r "
-                    "|cffcccccchttps://github.com/mod-playerbots/mod-playerbots|r");
-            }
-
-            if (sPlayerbotAIConfig.enabled || sPlayerbotAIConfig.randomBotAutologin)
-            {
-                std::string maxAllowedBotCount = std::to_string(sRandomPlayerbotMgr.GetMaxAllowedBotCount());
-
-                ChatHandler(player->GetSession()).SendSysMessage(
-                    "|cff00ff00Playerbots:|r The server is configured with " + maxAllowedBotCount + " bots.");
+                    "Server sources accessible here: |cffcccccchttps://github.com/19pvp/19pvp.github.io|r");
             }
         }
     }
